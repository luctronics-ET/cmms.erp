---
milestone: v1.0
audited: 2026-06-29
status: passed
scores:
  requirements: 18/18
  phases: 9/9
  integration: 6/6 flows
  flows: 6/6
gaps: []
gap_closed_during_audit:
  - id: "RES-05"
    issue: "POST /api/manutencao/uso and POST /api/manutencao/registro authenticated but missing the visualizador 403 guard (added in phases 1-2, before the phase-6 role guard). A visualizador could write uso_registros/ativo_plano_estado, corrupting cronograma demand."
    fix: "Added `if user.get('role')=='visualizador': raise HTTPException(403)` to both handlers (commit fix(audit))."
    verified: "8 manutencao tests green after fix; parity with the other 6 write routes restored."
tech_debt:
  - phase: 06-residuais-funcionais / 08-ajuda-e-documentacao
    items:
      - "tools/ one-time import/seed scripts (import_ata2, import_refrig_html, seed_refrig_preventivas, seed_completo) reference now-deleted legacy HTMLs — inert (data already seeded); test_import_ata2_climatizacao remains among the 12 pre-existing baseline failures."
      - "12 pre-existing test failures remain (10 unimplemented /api/catalogo/planos endpoints — out of scope; 1 ata2 import source removed; 1 sync legacy-table mismatch). Not introduced by this milestone; 2 of original 14 were closed in Phase 7."
      - "docs upload concurrent-race: narrow window where two simultaneous uploads to the same doc compute the same version; UNIQUE(documento_id,versao) + orphan-file cleanup handle failure, but BEGIN IMMEDIATE not applied (acceptable for single-admin LAN tool)."
      - "PROJECT.md per-phase evolution was deferred; requirements tracked via REQUIREMENTS.md traceability (all 18 Complete)."
---

# Milestone v1.0 — xCMASM ERP (Produção: Import + Hardening) — Audit

**Status: PASSED** — 18/18 requirements satisfied, 9/9 phases verified, 6/6 E2E flows wired. One integration blocker (RES-05 gap on two write routes) was found and fixed inline during the audit.

## Requirements Coverage (3-source: VERIFICATION + SUMMARY + traceability)

| Req | Phase | VERIFICATION | Traceability | Status |
|-----|-------|--------------|--------------|--------|
| IMP-01 | 1 | passed | Complete | satisfied |
| QA-02 | 1 | passed | Complete | satisfied |
| IMP-02 | 2 | passed | Complete | satisfied |
| IMP-03 | 3 | passed | Complete | satisfied |
| IMP-04 | 4 | passed | Complete | satisfied |
| IMP-05 | 5 | passed | Complete | satisfied |
| RES-01..05 | 6 | passed | Complete | satisfied (RES-05 completed during audit) |
| SEC-01, SEC-02, QA-01 | 7 | passed | Complete | satisfied |
| DOC-01..03 | 8 | passed | Complete | satisfied |
| CLN-01 | 9 | passed | Complete | satisfied |

## Cross-Phase Integration (integration checker)

All wiring verified end-to-end:
- **Cronograma (P5) ← equipe_config (P4) + ativo_plano_estado (P2) + criticidade/uso (P1/P6)** — real joins, not stubbed.
- **manut_registros (P2) → por_tempo vencimento (P6)** — column `data` matches MAX(data) read.
- **Argon2 login (P7) → token role → write guards** — composes correctly across manutencao + docs.
- **docs upload/download (P8)** — registered, frontend callers present, storage outside web tree.
- **help drawer (P8)** — topbar button → openAjuda(chave) → GET /api/docs/ajuda.
- **legacy cleanup (P9)** — zero stale .html references in active code; all 5 schema files in _SCHEMAS; all 5 routers registered, no collisions; `import backend.main` clean.

## E2E Flows
1. register uso → vencimento alert → plano checklist → registro updates proximo_uso → cronograma reflects it ✓
2. login (djb2→argon2 lazy upgrade) → role-gated writes (now all 8 manutencao write routes + docs) ✓
3. upload doc version → version increment + history → download ✓
4. help drawer per page ✓

## Notes
Tech debt itemized in frontmatter — none blocking. Full test suite: 138 passed / 12 pre-existing baseline failures (0 new regressions across the milestone).
