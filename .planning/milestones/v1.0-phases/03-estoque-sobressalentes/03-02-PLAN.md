---
phase: 03-estoque-sobressalentes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - assets/erp-manutencao.js
autonomous: false
requirements: [IMP-03]

must_haves:
  truths:
    - "A new 'Sobressalentes' tab appears in the Manutenção module (TAB_DEFS entry + RENDERERS renderer), mirroring how the 'Registrar Uso' tab was added in Phase 1"
    - "The tab lists peças with qtd_atual, unidade, a coloured badge (ZERADO/BAIXO/OK) and shows the total estimated stock value at the top"
    - "Buttons exist for +Nova peça (create), editar (edit), and Ajustar (modal with motivo + obs); all call the Phase-3 endpoints with a Bearer token from localStorage('xcmasm_token')"
    - "All server-supplied data is rendered via el()/textContent — no innerHTML of server data (safe DOM)"
  artifacts:
    - "assets/erp-manutencao.js — 'sobressalentes' TAB_DEFS entry + async 'sobressalentes'(cont) renderer with sb_-prefixed helpers"
  key_links:
    - "renderer fetches GET /api/manutencao/sobressalentes and renders badge + valor_estimado_total from the server response (server is source of truth for both)"
    - "Ajustar modal POSTs to /sobressalentes/{id}/ajuste then re-renders the list so qtd + valor update without reload"
---

<objective>
Frontend vertical slice completing IMP-03: a new "Sobressalentes" tab in the Manutenção module that lists local spare parts with status badges and total estimated value, and lets the técnico add, edit, and adjust quantity (modal with motivo + obs) against the Phase-3 backend. Visual ported from the approved legacy `.docs_cmasm/referencias/CMASM_Gestao_v2.html`, dark-theme tokens only, safe DOM via el()/textContent.

Implements IMP-03 (frontend half) per locked decisions in 03-CONTEXT.md.

Purpose: Técnico consults and manages local spares end-to-end; closes the IMP-03 user story.
Output: a working, API-backed tab — no HTML scaffolding changes needed (rendering is fully RENDERERS-driven, as in Phase 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estoque-sobressalentes/03-CONTEXT.md
@.planning/phases/03-estoque-sobressalentes/03-01-PLAN.md

# Pattern to mirror exactly — the Registrar Uso tab (TAB_DEFS + async renderer + ru_ helpers):
@assets/erp-manutencao.js
@.planning/phases/01-registrar-uso/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'Sobressalentes' tab — TAB_DEFS entry + API-backed renderer</name>
  <files>assets/erp-manutencao.js</files>
  <read_first>
    assets/erp-manutencao.js:
      - TAB_DEFS array (~line 23) — add a new entry exactly like the 'registrar-uso' one at line 29.
      - The async 'registrar-uso'(cont) renderer (~lines 1284-1490) — copy its structure: pull { el } from window.engine.utils; define sb_-prefixed local helpers (sbToken/sbAuthHeaders mirroring ruToken/ruAuthHeaders); use apiUrl(path) for fetch URLs; build everything with el() and replaceChildren; dark-theme CSS tokens only (var(--panel), var(--line), var(--ink), var(--ink-2), var(--ink-3), var(--acc), var(--green), var(--red), var(--amber), var(--bg2), var(--bg3)); use the existing toast() for errors.
      - apiUrl helper (~line 324).
  </read_first>
  <action>
    Add { id: 'sobressalentes', icon: '🔩', label: 'Sobressalentes' } to TAB_DEFS (after the 'registrar-uso' entry).

    Add an async 'sobressalentes'(cont) method inside the RENDERERS object (place it right after the 'registrar-uso' renderer). Use sb_-prefixed local helpers to avoid global collisions (per the Phase-1 ru_ convention). On render:
      1. GET apiUrl('/api/manutencao/sobressalentes') with Authorization: Bearer + localStorage('xcmasm_token').
      2. Render a header card showing "Valor estimado do estoque: R$ X" from the response's valor_estimado_total (formatted), plus a "+ Nova peça" button.
      3. Render the list as a table built with el(): columns nome, categoria, qtd_atual + unidade, badge, preço, actions. The badge is a coloured pill driven by the server's badge field — green=OK, amber=BAIXO, red=ZERADO (map badge→token color). Every cell uses el()/textContent — NEVER innerHTML of server data. (innerHTML is allowed only for static placeholder strings with no server data, exactly as the registrar-uso renderer does for "Carregando..." text.)
      4. Row actions: "Editar" (opens the create/edit form prefilled) and "Ajustar" (opens the ajuste modal — Task 2).
    Re-fetch + re-render after any successful mutation so qtd and total value stay current.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const fs=require('fs'); const s=fs.readFileSync('assets/erp-manutencao.js','utf8'); if(!/id:\s*'sobressalentes'/.test(s))throw new Error('no TAB_DEFS entry'); if(!/'sobressalentes'\s*\(cont\)/.test(s))throw new Error('no renderer'); if(!/\/api\/manutencao\/sobressalentes/.test(s))throw new Error('no fetch'); console.log('OK')"</automated>
  </verify>
  <done>New tab visible in Manutenção; list renders peças with badge + total estimated value pulled from the server; all server data rendered via el()/textContent.</done>
</task>

<task type="auto">
  <name>Task 2: +Nova peça / Editar form + Ajustar modal (motivo + obs)</name>
  <files>assets/erp-manutencao.js</files>
  <read_first>
    assets/erp-manutencao.js — the same 'sobressalentes' renderer from Task 1. Reuse the el()/inputStyle/cardStyle patterns from the 'registrar-uso' renderer (~lines 1363-1403). Reuse the existing modal/toast helpers used elsewhere in the file (search for the modal helper used by the planos renderer near m.close()).
  </read_first>
  <action>
    Implement three flows inside the 'sobressalentes' renderer using sb_-prefixed handlers:

    "+ Nova peça" / "Editar": open a form (modal or inline panel, matching the module's existing modal helper) with fields nome (required), codigo, categoria (select: consumivel|sobressalente|ferramenta), unidade, qtd_minima, preco_unitario, obs. Save → POST apiUrl('/api/manutencao/sobressalentes') for new, or PUT apiUrl('/api/manutencao/sobressalentes/'+id) for edit, with Bearer token. On success: toast green, close, re-render the list.

    "Ajustar": open a modal with fields tipo (select: entrada|saida|ajuste), quantidade (number), motivo (required text), obs (optional text). Save → POST apiUrl('/api/manutencao/sobressalentes/'+id+'/ajuste') with {quantidade, tipo, motivo, obs} and Bearer token. On success: toast green, close modal, re-render so qtd_atual + valor_estimado_total + badge update. Surface backend 4xx detail via toast (red) — read the JSON `detail` field.

    Validate required fields client-side (advisory; server is authoritative). Never send operador from the client — it comes from the token server-side.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const fs=require('fs'); const s=fs.readFileSync('assets/erp-manutencao.js','utf8'); if(!/ajuste/.test(s))throw new Error('no ajuste call'); if(!/motivo/.test(s))throw new Error('no motivo field'); console.log('OK')"</automated>
  </verify>
  <done>+Nova peça, Editar, and Ajustar(modal motivo+obs) all work against the Phase-3 endpoints with a Bearer token; the list re-renders after each mutation; backend error detail surfaced via toast.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Sobressalentes tab end-to-end</name>
  <files>(manual verification — no files changed)</files>
  <action>Pause for human verification of the Sobressalentes tab in a running app. See how-to-verify for the exact steps; resume only after the operator confirms.</action>
  <what-built>The "Sobressalentes" tab in the Manutenção module: list with ZERADO/BAIXO/OK badges and total estimated stock value, +Nova peça / Editar forms, and an Ajustar modal (motivo + obs) — all wired to the Phase-3 backend.</what-built>
  <how-to-verify>
    1. Serve the app: `cd /home/luc/DEV_ERP/cmasm.erp && python3 -m http.server 8080` and start the backend `uvicorn backend.main:app --port 8010 --reload`.
    2. Open the ERP, log in, go to Manutenção → click the new "Sobressalentes" tab.
    3. Click "+ Nova peça", create a peça (set qtd_minima and preço) → it appears in the list with the correct badge and the total estimated value updates.
    4. Click "Ajustar" on that peça, choose tipo, enter quantidade + motivo + obs, save → qtd_atual changes, badge/total update without a page reload.
    5. Click "Editar", change preço, save → value updates.
    6. Confirm the central estoque screen (Estoque) still shows exactly the same items as before — no spare parts leaked in.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify><human-check>Operator confirms the tab lists peças with badges + total value and that create/edit/ajuste work, with the central estoque unchanged.</human-check></verify>
  <done>Operator types "approved" after confirming all how-to-verify steps pass.</done>
</task>

</tasks>

<verification>
- New 'sobressalentes' tab renders and lists peças with badges + total value (IMP-03 SC1).
- +Nova peça / Editar / Ajustar(modal) work against Phase-3 endpoints (IMP-03 SC2).
- Existing smoke tests for cmasm_erp.html still pass (no HTML scaffolding change; rendering is RENDERERS-driven, as in Phase 1).
</verification>

<success_criteria>
- IMP-03 user story closed end-to-end: técnico opens "Sobressalentes", sees badges + estimated value, and can add/edit/adjust with a movement logged.
- No innerHTML of server data (safe DOM via el()/textContent).
- Central estoque visually unchanged (SC3 confirmed at the human-verify checkpoint; SC3/SC4 proven by test_sobressalentes in 03-01).
</success_criteria>

<output>
Create `.planning/phases/03-estoque-sobressalentes/03-02-SUMMARY.md` when done.
</output>