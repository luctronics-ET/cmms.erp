---
phase: 03-estoque-sobressalentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/schema_manutencao.sql
  - backend/manutencao.py
  - tests/test_manutencao.py
autonomous: true
requirements: [IMP-03]

must_haves:
  truths:
    - "GET /api/manutencao/sobressalentes returns each peça with a computed badge (ZERADO|BAIXO|OK) and the response carries a total valor_estimado = Σ qtd_atual×preco_unitario"
    - "POST /api/manutencao/sobressalentes creates a peça; PUT /{id} edits it; both require a non-visitante token"
    - "POST /api/manutencao/sobressalentes/{id}/ajuste updates qtd_atual AND inserts one sobressalentes_movimentos row in a single atomic transaction with operador taken from the token (never the payload)"
    - "GET /api/manutencao/sobressalentes/{id}/movimentos returns the movement history newest-first"
    - "GET /api/estoque returns exactly the same records as before — no sobressalentes leak into the estoque/estoque_movimentos tables"
  artifacts:
    - "data/schema_manutencao.sql — sobressalentes + sobressalentes_movimentos tables (CREATE TABLE IF NOT EXISTS, additive)"
    - "backend/manutencao.py — 5 sobressalentes endpoints with _require_auth"
    - "tests/test_manutencao.py — test_sobressalentes (CRUD + atomic ajuste + estoque-isolation)"
  key_links:
    - "ajuste handler uses one aiosqlite.connect block + single commit (mirror registrar_uso) so qtd update and movimento insert never partially apply"
    - "schema_manutencao.sql is already in CoreDB._SCHEMAS — new tables load automatically on db.init() with no db_core.py change"
---

<objective>
Backend vertical slice for the local spare-parts inventory (estoque de sobressalentes), kept fully SEPARATE from the central `estoque` table. Adds two dedicated tables, five authenticated endpoints (list+badge+valor, create, edit, atomic ajuste with movement log, history), and a self-contained pytest covering CRUD, atomic adjustment, and proof that the central estoque is untouched.

Implements IMP-03 (backend half) per locked decisions D in 03-CONTEXT.md (SEPARATE tables, NOT a scope column on estoque).

Purpose: Técnicos manage their local spares without touching the central warehouse; success criteria require zero interference with `GET /api/estoque`.
Output: schema_manutencao.sql tables, manutencao.py endpoints, test_sobressalentes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estoque-sobressalentes/03-CONTEXT.md

# Patterns to mirror exactly:
@backend/manutencao.py
@data/schema_manutencao.sql
@tests/test_manutencao.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sobressalentes + sobressalentes_movimentos tables</name>
  <files>data/schema_manutencao.sql</files>
  <read_first>
    data/schema_manutencao.sql (append after the Fase 02 block at the end; mirror its CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS style).
    data/schema_core.sql lines 126-150 — `estoque`/`estoque_movimentos` are the SHAPE reference ONLY. Do NOT modify them.
  </read_first>
  <action>
    Append a new "Fase 03" section to the END of data/schema_manutencao.sql (additive — never DROP, never ALTER existing tables). Add two tables, both with CREATE TABLE IF NOT EXISTS.

    Table `sobressalentes` columns per D (CONTEXT.md): id INTEGER PRIMARY KEY AUTOINCREMENT; codigo TEXT UNIQUE (nullable); nome TEXT NOT NULL; categoria TEXT NOT NULL DEFAULT 'sobressalente' (vocabulary aligned with refrigeração: consumivel|sobressalente|ferramenta); unidade TEXT NOT NULL DEFAULT 'un'; qtd_atual REAL NOT NULL DEFAULT 0; qtd_minima REAL NOT NULL DEFAULT 0; preco_unitario REAL DEFAULT 0; obs TEXT; ativo INTEGER DEFAULT 1; criado_em TEXT NOT NULL DEFAULT (datetime('now')).

    Table `sobressalentes_movimentos` columns: id INTEGER PRIMARY KEY AUTOINCREMENT; item_id INTEGER NOT NULL REFERENCES sobressalentes(id); tipo TEXT NOT NULL (entrada|saida|ajuste); quantidade REAL NOT NULL; motivo TEXT; obs TEXT; operador TEXT; created_at TEXT NOT NULL DEFAULT (datetime('now')).

    Add CREATE INDEX IF NOT EXISTS on sobressalentes_movimentos(item_id, created_at DESC). Document column intent with brief inline comments matching the file's existing style. NOTE: schema_manutencao.sql is already in CoreDB._SCHEMAS — no db_core.py edit is needed.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import sqlite3,tempfile,os; d=tempfile.mkdtemp(); p=os.path.join(d,'t.db'); c=sqlite3.connect(p); sql=open('data/schema_manutencao.sql').read(); c.executescript(sql); c.executescript(sql); cols={r[1] for r in c.execute('PRAGMA table_info(sobressalentes)')}; assert {'id','codigo','nome','categoria','unidade','qtd_atual','qtd_minima','preco_unitario','obs','ativo','criado_em'} <= cols, cols; mcols={r[1] for r in c.execute('PRAGMA table_info(sobressalentes_movimentos)')}; assert {'id','item_id','tipo','quantidade','motivo','obs','operador','created_at'} <= mcols, mcols; print('OK idempotent + columns present')"</automated>
  </verify>
  <done>Both tables exist after running the schema twice (idempotent, no "table already exists"/"duplicate column" error); all columns from D present; no change to estoque/estoque_movimentos.</done>
</task>

<task type="auto">
  <name>Task 2: Five sobressalentes endpoints in manutencao.py</name>
  <files>backend/manutencao.py</files>
  <read_first>
    backend/manutencao.py — reuse _db(), _require_auth(), and the atomic-transaction pattern from registrar_uso (single aiosqlite.connect block + single commit, operador derived from the token via user.get("mat") or nome). Append the new code at the END of the file. Add a Pydantic BaseModel for create/edit/ajuste mirroring UsoIn/RegistroIn style with field_validator guards.
  </read_first>
  <action>
    Add five endpoints on the existing router (prefix /api/manutencao), all calling _require_auth(authorization) first.

    GET /sobressalentes (optional query categoria): fetch active rows (ativo=1) from sobressalentes via _db().fetch_all, parameterized. For each row compute badge: 'ZERADO' if qtd_atual<=0, else 'BAIXO' if qtd_atual<qtd_minima, else 'OK'. Return an object containing the items list (each with its computed badge) and valor_estimado_total = sum of qtd_atual*preco_unitario across the returned rows.

    POST /sobressalentes (status 201): create a peça from a validated body (nome required non-empty via field_validator; categoria/unidade defaulted; numeric fields default 0). Insert via the atomic-connection pattern; return the new id. Reject empty nome with 422 (Pydantic). Writes require a non-visitante token — after _require_auth, if user role == 'visualizador' raise HTTPException(403) (consistent with the rest of the system).

    PUT /sobressalentes/{id}: update editable columns (nome, codigo, categoria, unidade, qtd_minima, preco_unitario, obs) of an existing peça; 404 if not found; 403 for visualizador. Does NOT change qtd_atual (that is the ajuste path only).

    POST /sobressalentes/{id}/ajuste (status 201): payload {quantidade, tipo, motivo, obs?} where tipo in entrada|saida|ajuste. In ONE aiosqlite.connect block with a SINGLE commit: read the row inside the txn (404 if missing), compute the new qtd_atual (entrada adds, saida subtracts, ajuste sets-by-delta — document the chosen semantics in a docstring; clamp not required but never write below 0 for saida — raise 422 if it would go negative), UPDATE sobressalentes.qtd_atual, and INSERT one sobressalentes_movimentos row with operador taken from the token (NOT the payload). Return the new qtd_atual. 403 for visualizador.

    GET /sobressalentes/{id}/movimentos: return history for the item, newest-first (ORDER BY created_at DESC), parameterized, with an optional limit (Query default 50, ge 1, le 200).
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import ast; ast.parse(open('backend/manutencao.py').read()); print('syntax OK')" && grep -Eq "sobressalentes/\{[a-z_]+\}/ajuste" backend/manutencao.py && grep -v '^[[:space:]]*#' backend/manutencao.py | grep -q "_require_auth" && echo "endpoints present"</automated>
  </verify>
  <done>Five endpoints registered under /api/manutencao; ajuste does UPDATE+INSERT in one commit; operador comes from the token; valor_estimado_total and badge computed server-side; visualizador blocked on writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_sobressalentes — CRUD + atomic ajuste + estoque isolation</name>
  <files>tests/test_manutencao.py</files>
  <read_first>
    tests/test_manutencao.py — reuse _query, _exec, _seed_user, _seed_sessao, _auth helpers already at the top of the file. Mirror the existing test style (sync app_client fixture from conftest.py; sqlite3 direct DB assertions). Append the new test at the END of the file.
  </read_first>
  <behavior>
    - Create: POST /api/manutencao/sobressalentes with auth → 201, returns id; row exists in sobressalentes with the posted nome/categoria.
    - List+badge+valor: seed two peças (one qtd_atual=0 → ZERADO, one qtd_atual>=qtd_minima → OK with preco_unitario set); GET /api/manutencao/sobressalentes → badges correct and valor_estimado_total == Σ qtd×preco.
    - Edit: PUT /{id} changes nome/preco; DB reflects change; qtd_atual unchanged by PUT.
    - Ajuste atomic: POST /{id}/ajuste {quantidade,tipo,motivo} → qtd_atual updated AND exactly one sobressalentes_movimentos row inserted with operador set and the movement's quantidade/tipo/motivo persisted.
    - Estoque isolation: count rows in `estoque` before and after the whole test → unchanged; GET /api/estoque (if reachable) returns the same set; assert no sobressalentes peça id appears in estoque.
    - Auth: a write endpoint without Bearer token → 401.
  </behavior>
  <action>
    Add async (or sync, matching the existing tests in this file) test function test_sobressalentes using the app_client fixture and the existing seed/auth helpers. Run on a clean test DB. Cover every behavior above. For the estoque-isolation assertion, snapshot SELECT count(*) FROM estoque (and the set of ids) before and after, asserting equality — proving the new feature writes only to sobressalentes/sobressalentes_movimentos. Do NOT seed or mutate estoque.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py::test_sobressalentes -x -q</automated>
  </verify>
  <done>test_sobressalentes passes on a clean DB; the full tests/test_manutencao.py file passes with no regression vs the 14 known pre-existing failures (which are outside this file).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/manutencao/sobressalentes* | Untrusted JSON payloads (quantities, free-text motivo/obs) cross here |
| token → operador identity | Operador must be derived from the validated session, never from the request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | All SQL in the 5 endpoints | high | mitigate | Parameterized queries only (mirror registrar_uso); no string interpolation of payload into SQL |
| T-03-02 | Spoofing | ajuste operador | high | mitigate | operador read from _require_auth session (user mat/nome), never from payload |
| T-03-03 | Tampering | ajuste qtd vs movimento | high | mitigate | Single aiosqlite.connect block + one commit so UPDATE qtd_atual and INSERT movimento never partially apply |
| T-03-04 | Elevation | write endpoints | medium | mitigate | role == 'visualizador' → HTTPException(403) on POST/PUT/ajuste |
| T-03-05 | Tampering | central estoque integrity | high | mitigate | Feature writes only to sobressalentes/sobressalentes_movimentos; test asserts estoque row-set unchanged |
| T-03-06 | DoS | negative-going saida | low | accept | 422 when a saida would drive qtd_atual below 0; no further rate limiting (internal closed network) |
</threat_model>

<verification>
- `python -m pytest tests/test_manutencao.py -q` → green for this file (no in-file regression).
- Schema runs twice without error (idempotent, additive).
- `grep` confirms no edit to estoque/estoque_movimentos in this diff.
</verification>

<success_criteria>
- sobressalentes + sobressalentes_movimentos tables created additively (IMP-03 SC3: no table mixing).
- Five authenticated endpoints implement list+badge+valor / create / edit / atomic ajuste+log / history (IMP-03 SC1, SC2).
- test_sobressalentes green on clean DB; GET /api/estoque records unchanged (IMP-03 SC3, SC4).
</success_criteria>

<output>
Create `.planning/phases/03-estoque-sobressalentes/03-01-SUMMARY.md` when done.
</output>
