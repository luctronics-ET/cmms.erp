---
phase: 05-cronograma-preventivo
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - assets/erp-manutencao.js
autonomous: true
requirements: [IMP-05]
must_haves:
  truths:
    - "A new 'Cronograma' tab appears in the Manutenção nav; opening it fetches GET /api/manutencao/cronograma with a Bearer token and renders the day-by-day schedule"
    - "Each day shows its scheduled assets with a criticality badge (dark-theme colors), estimated duration, and a capacity-used-vs-available bar; a KPI header row shows total OS, horas-pessoa, dias úteis, data de conclusão and % utilização"
    - "When kpis.alerta is true, a red alert banner is shown (demand > capacity); reloading the tab after changing the Fase 4 crew config reflects the new schedule"
  artifacts:
    - "assets/erp-manutencao.js — 'cronograma' entry in TAB_DEFS + async 'cronograma'(cont) renderer in RENDERERS"
  key_links:
    - "RENDERERS['cronograma'] ← renderActiveTab() dispatch ← TAB_DEFS 'cronograma' tab button"
    - "fetch(apiUrl('/api/manutencao/cronograma')) + Bearer token (localStorage xcmasm_token) ← Wave 1 endpoint"
    - "data.dias[].itens[].criticidade → CRIT_COLOR_DARK badge; horas_usadas/horas_disponiveis → capacity bar width"
---

<objective>
Add the "Cronograma" tab to the Manutenção module in `assets/erp-manutencao.js`: a nav entry in TAB_DEFS and an `async 'cronograma'(cont)` renderer in the RENDERERS map that fetches the Wave 1 endpoint and renders a dark-theme day-by-day schedule with criticality badges, per-day capacity bars, a KPI header, and a red alert banner when demand exceeds capacity (per D: "Nova aba Cronograma; vista dia-a-dia; KPIs no topo; alerta vermelho quando demanda>capacidade; fetch+Bearer; DOM seguro via el()/textContent").

Purpose: Completes the IMP-05 vertical slice — the gestor can now SEE the computed preventive schedule. After Wave 1 the data exists; after this task a real user opens a tab and reads their mobilization plan.
Output: Updated `assets/erp-manutencao.js` (one TAB_DEFS entry + one renderer method).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# JSON shape (dias/kpis) and badge-color mapping for the renderer.
@.planning/phases/05-cronograma-preventivo/05-RESEARCH.md
@.planning/phases/05-cronograma-preventivo/05-CONTEXT.md

# Wave 1 contract — read this to bind the renderer to the actual {dias,kpis} JSON shape.
@.planning/phases/05-cronograma-preventivo/05-01-SUMMARY.md

# Patterns to MIRROR: TAB_DEFS (lines 23-32), renderActiveTab dispatch (lines 417-430),
# RENDERERS object (line 572), the 'equipe-tecnica' async renderer (line 1943) for the
# token/auth-headers/fetch/el()/card-style conventions, apiUrl (line 326).
@assets/erp-manutencao.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the Cronograma tab in TAB_DEFS</name>
  <read_first>
    - assets/erp-manutencao.js lines 23-32 (TAB_DEFS array — add one entry after 'equipe-tecnica')
    - assets/erp-manutencao.js lines 417-430 (renderActiveTab — dispatches RENDERERS[id], so the tab id must equal the renderer key)
  </read_first>
  <files>assets/erp-manutencao.js</files>
  <action>
    Add one entry to the TAB_DEFS array immediately after the 'equipe-tecnica' entry (per D and RESEARCH "Frontend: where to add the Cronograma tab"): { id: 'cronograma', icon: '📅', label: 'Cronograma' }. The id 'cronograma' must exactly match the renderer key added in Task 2 so renderActiveTab dispatches to it. Do not reorder or modify the existing entries.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && grep -v '^[[:space:]]*//' assets/erp-manutencao.js | grep -c "id: 'cronograma'"</automated>
  </verify>
  <done>TAB_DEFS contains a 'cronograma' tab (📅 Cronograma) positioned after 'equipe-tecnica'; existing tab entries unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: 'cronograma' renderer — fetch, KPI header, alert banner, day-by-day view</name>
  <read_first>
    - .planning/phases/05-cronograma-preventivo/05-01-SUMMARY.md (exact {dias,kpis} JSON shape from Wave 1)
    - .planning/phases/05-cronograma-preventivo/05-RESEARCH.md section "Frontend: Tab and Renderer" (renderer skeleton + CRIT_COLOR_DARK mapping)
    - assets/erp-manutencao.js lines 1943-2010 ('equipe-tecnica' renderer — copy its eqToken()/eqAuthHeaders()/cardStyle/el() conventions; this is the canonical top-level async tab renderer to mirror)
    - assets/erp-manutencao.js line 326 (apiUrl) and line 572 (RENDERERS object — add the new method here)
  </read_first>
  <files>assets/erp-manutencao.js</files>
  <behavior>
    - Opening the Cronograma tab issues GET apiUrl('/api/manutencao/cronograma') with header Authorization: 'Bearer ' + localStorage xcmasm_token; a loading placeholder shows while fetching
    - On 200: a KPI header row renders total_os, horas_pessoa, dias_uteis, data_conclusao, pct_utilizacao; each day in dias renders a header (date + dia_semana + "N OS · X.Xh / Y.Yh"), a capacity bar whose fill width = horas_usadas/horas_disponiveis×100% (clamped ≤100), and its itens each with a criticidade badge (CRIT_COLOR_DARK), nome, and duracao_min
    - When kpis.alerta is true: a red banner ("Demanda excede a capacidade") renders above the days
    - On non-OK response or fetch error: a readable error message renders in the tab (no uncaught exception)
    - All asset names and labels go through textContent / el() text args — never innerHTML for server data
  </behavior>
  <action>
    Add an async 'cronograma'(cont) method to the RENDERERS object (mirror the 'equipe-tecnica' method's structure). Per D (vista dia-a-dia; badge de criticidade cores dark; barra capacidade usada vs disponível; KPIs no topo; alerta vermelho; fetch+Bearer; DOM seguro via el()/textContent; recarrega ao abrir):

    1. Set state._scopeCats = null (this is a global tab, not category-scoped — same as 'equipe-tecnica').
    2. Local crToken()/crAuthHeaders() helpers reading localStorage 'xcmasm_token' (copy the eqToken pattern). Render a "Calculando cronograma…" placeholder into cont first.
    3. fetch(apiUrl('/api/manutencao/cronograma'), { headers: { Authorization: 'Bearer ' + crToken() } }). If !r.ok, replace cont with an error card (status + detail when available) and return. Wrap in try/catch and render a toast/error card on network failure.
    4. Parse { dias, kpis }. Build a KPI header row of small cards (reuse the cardStyle/labelStyle conventions from 'equipe-tecnica'): Total OS = kpis.total_os, Horas-pessoa = kpis.horas_pessoa, Dias úteis = kpis.dias_uteis, Conclusão = kpis.data_conclusao, Utilização = kpis.pct_utilizacao + '%'.
    5. If kpis.alerta, prepend a red banner (background var(--red) tint, border var(--red)) with text "Demanda excede a capacidade da equipe no horizonte".
    6. For each dia in dias: a day card with header (dia.data + ' · ' + dia.dia_semana, then dia.itens.length + ' OS · ' + dia.horas_usadas + 'h / ' + dia.horas_disponiveis + 'h'); a capacity bar = an outer track div + inner fill div whose style.width = Math.min(100, dia.horas_usadas/dia.horas_disponiveis×100) + '%' (guard horas_disponiveis>0); then dia.itens mapped to rows, each with a criticidade badge (CRIT_COLOR_DARK[item.criticidade] || var(--ink-3)), the asset nome (textContent), and the duração (item.duracao_min + ' min', or duracao_h + 'h').

    Define CRIT_COLOR_DARK inside the renderer (or module-local) mapping CRÍTICA→var(--red), ALTA→var(--amber), MÉDIA→var(--acc), BAIXA→var(--green) — dark theme per CLAUDE.md and RESEARCH (do NOT use the legacy gov.br light palette). Use only el() and textContent for all server-supplied strings (asset names, criticidade) — never innerHTML.
  </action>
  <verify>
    <human-check>
      Start the backend (uvicorn backend.main:app --port 8010 --reload) with seeded data, open the ERP, go to Manutenção → Cronograma. Confirm: (1) the day-by-day list renders with criticality badges in dark-theme colors and a capacity bar per day, (2) the KPI header shows total OS / horas-pessoa / dias úteis / conclusão / % utilização, (3) the red alert banner appears when demand exceeds capacity (e.g. set a tiny crew config in Equipe Técnica and reload), (4) changing the Fase 4 crew config and reopening the tab changes the schedule.
    </human-check>
  </verify>
  <done>RENDERERS['cronograma'] fetches the endpoint with a Bearer token, renders a KPI header, a per-day list with dark-theme criticality badges + duration + capacity bars, and a red alert banner when kpis.alerta is true; all server strings rendered via el()/textContent; error/loading states handled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → DOM | Asset names / criticidade strings from the endpoint are rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering (XSS) | day-by-day renderer | medium | mitigate | All server-supplied strings rendered via el()/textContent — never innerHTML (Task 2) |
| T-05-06 | Information Disclosure | fetch auth | low | mitigate | Bearer token from localStorage sent on the GET; endpoint enforces _require_auth (Wave 1) |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — vanilla JS, no build step (RESEARCH: no new packages) |
</threat_model>

<verification>
- `grep -c "'cronograma'" assets/erp-manutencao.js` shows the TAB_DEFS entry and the RENDERERS method.
- Manual: Manutenção → Cronograma renders day cards, KPI header, capacity bars, and the alert banner on demand>capacity (human-check above).
- No `innerHTML =` introduced for server data in the new renderer (grep the new block).
</verification>

<success_criteria>
- The Cronograma tab is reachable from the Manutenção nav and renders the Wave 1 endpoint's {dias,kpis} as a dark-theme day-by-day schedule with KPIs and capacity bars.
- A red alert banner appears when kpis.alerta is true.
- Reloading the tab after a Fase 4 crew-config change reflects the new schedule.
- DOM safety preserved: server strings via el()/textContent only.
</success_criteria>

<output>
Create `.planning/phases/05-cronograma-preventivo/05-02-SUMMARY.md` when done — note the tab id, the renderer key, and any deviations from the Wave 1 JSON shape.
</output>
