---
phase: 05-cronograma-preventivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/manutencao.py
  - tests/test_manutencao.py
autonomous: true
requirements: [IMP-05]
must_haves:
  truths:
    - "GET /api/manutencao/cronograma returns a dia-by-dia greedy-packed schedule with a kpis block, requiring a Bearer token (401 otherwise)"
    - "For a fixed asset dataset + crew config + injected today=YYYY-MM-DD, the response is byte-for-byte deterministic (stable sort criticidade→falta→ativo_id, deterministic day packing, exact KPIs)"
    - "When total demand minutes exceed total available capacity over the schedule horizon, kpis.alerta is true"
  artifacts:
    - "backend/manutencao.py — GET /cronograma endpoint + _est_duracao_min + computar_cronograma + demand query helpers"
    - "tests/test_manutencao.py — test_cronograma (happy path, exact KPIs) + test_cronograma_alerta (demand>capacity) + test_cronograma_requires_auth"
  key_links:
    - "equipe_config (Fase 4) → _capacidade(config).h_dia_total → cap_dia_min drives per-day packing"
    - "ativo_plano_estado (Fase 2) proximo_uso vs ativos.uso_atual → falta → sort key + demand membership"
    - "today query param → date.fromisoformat → working-day cursor (DOW_STR→Python weekday map)"
---

<objective>
Port the legacy `pmocCronograma()` greedy day-packing scheduler into a read-only computed endpoint `GET /api/manutencao/cronograma?categoria=&today=` in `backend/manutencao.py`, plus deterministic tests. No new tables — the endpoint reads existing `ativos`, `ativo_plano_estado`, `pmoc_*`, and `equipe_config` rows and computes the schedule on the fly (per D: "Endpoint read-only computado; Sem nova tabela").

Purpose: Delivers the IMP-05 backend slice — the gestor's preventive-maintenance schedule, packed by crew capacity (Fase 4), ordered by criticidade, with mobilization KPIs. This is the data contract the Wave 2 "Cronograma" tab renders.
Output: New endpoint + pure scheduling helpers in `backend/manutencao.py`; three new tests in `tests/test_manutencao.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE algorithm, exact constants, demand SQL, sort key, determinism pitfalls, and the
# fixed test dataset with expected output are fully specified here — read it first.
@.planning/phases/05-cronograma-preventivo/05-RESEARCH.md
@.planning/phases/05-cronograma-preventivo/05-CONTEXT.md

# Existing helpers to REUSE (do not re-implement): _require_auth (line 32),
# _DEFAULT_CONFIG (line 1016), _capacidade (line 1024), _db (line 27),
# GET /equipe/config config-loading pattern (lines 1181-1207).
@backend/manutencao.py

# Test infra to REUSE: _auth(main), _exec(main, sql, params), _query(main, sql),
# app_client fixture (sync TestClient).
@tests/test_manutencao.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scheduling helpers — duration formula, demand query, greedy packing</name>
  <read_first>
    - .planning/phases/05-cronograma-preventivo/05-RESEARCH.md sections "The Exact Legacy Algorithm", "Duration Estimation", "Demand Query (SQL)", "Sort Key — Determinism Contract", "Greedy Packing Algorithm — Python Port", "Determinism Pitfalls"
    - backend/manutencao.py lines 1016-1066 (_DEFAULT_CONFIG, _capacidade — reuse, do not duplicate)
    - backend/manutencao.py lines 32-44 (_require_auth — reuse in Task 2)
  </read_first>
  <files>backend/manutencao.py</files>
  <behavior>
    - _est_duracao_min("SPLIT") == 105; _est_duracao_min("PISO/TETO") == 119; _est_duracao_min("JANELA") == 57; _est_duracao_min("SELF CONTAINED") == 207; _est_duracao_min("VIATURA_QUALQUER") == 105 (unknown tipo → _DEFAULT n=9, f_eq=1.0)
    - DOW map: "seg"→0, "ter"→1, "qua"→2, "qui"→3, "sex"→4, "sab"→5, "dom"→6 (Python weekday, Mon=0)
    - Sort key is the 3-tuple (CRIT_ORDER.get(crit, 2), falta, ativo_id); CRIT_ORDER maps the accented forms CRÍTICA→0, ALTA→1, MÉDIA→2, BAIXA→3; unknown/operacional/None → 2
    - computar_cronograma packs jobs ≤ cap_dia_min into a day, overflows to next working day only when the current day already holds ≥1 item, advances the cursor only across working days, pops a trailing empty day, and never loops forever (MAX_JOBS / MAX_DAYS guards; cap_dia_min floored to ≥1)
    - KPIs: total_os=len(fila); horas_pessoa=round(Σmin/60,1); dias_uteis=len(dias); data_conclusao=last day ISO or None; pct_utilizacao=round(Σmin/(cap_dia_min×dias_uteis)×100,1); alerta = Σmin > cap_dia_min×dias_uteis
  </behavior>
  <action>
    Append four pure helpers plus their module-level constants to backend/manutencao.py (after _capacidade, before the Endpoints section). Per D (sort estável criticidade→falta→id; capacidade=equipe_config; duração=legado; greedy packing; determinístico):

    1. Module constants exactly per RESEARCH "Duration Estimation" and "Greedy Packing Algorithm" sections: MIN_POR_ITEM=10, SETUP_MIN=15, FATOR_TIPO_EQUIP, FATOR_MANUT, N_CHECKLIST (with the "_DEFAULT": 9 SPLIT-baseline fallback), CRIT_ORDER, a DOW string→Python-weekday dict, MAX_JOBS=5000, MAX_DAYS=365, DEFAULT_CAP=240. Use the exact numeric constants tabulated in RESEARCH — do not invent values.

    2. _est_duracao_min(tipo, tipo_manut="PREVENTIVA") -> int: round(N_CHECKLIST.get(tipo, _DEFAULT) × MIN_POR_ITEM × FATOR_TIPO_EQUIP.get(tipo,1.0) × FATOR_MANUT.get(tipo_manut,1.0) + SETUP_MIN). Exact port of legacy estTempoServico.

    3. A criticidade normalizer that maps stored values to the four accented canonical forms, treating the migration default 'operacional', None, and any unrecognized string as 'MÉDIA' (sort rank 2). Match the DB's accented Unicode forms exactly (Pitfall 4 in RESEARCH).

    4. _dias_uteis_set(dias_semana) -> set[int] using the DOW map; empty/invalid input falls back to {0,1,2,3,4} (Mon–Fri).

    5. computar_cronograma(fila, cap_dia_min, dias_uteis_py, today) -> (dias, kpis): a faithful port of the RESEARCH "Greedy Packing Algorithm — Python Port" skeleton. cap_dia_min floored to max(...,1); the inner novo_dia advances the cursor across non-working days, records the day, then steps the cursor one day forward (mirrors the legacy advance-after-record order); overflow opens a new day only when the current day already has items; pop a trailing empty day; honor MAX_JOBS and MAX_DAYS guards so a zero/garbage capacity can never hang. Each day dict carries data (ISO), dia_semana (pt-BR abbrev "Seg"/"Ter"/… per RESEARCH A4), itens (the job dicts unchanged), horas_usadas (round, 2dp), horas_disponiveis (round cap_dia_min/60, 2dp). KPIs exactly per the behavior block and RESEARCH "Step 5". Use / (true division) everywhere in KPI math, never // (Pitfall 5).

    Keep these helpers pure (no DB, no auth) so Task 3 can unit-test them directly and Task 2 can call them. Do NOT print, log, or persist anything.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import backend.manutencao as m; assert m._est_duracao_min('SPLIT')==105 and m._est_duracao_min('JANELA')==57 and m._est_duracao_min('SELF CONTAINED')==207 and m._est_duracao_min('ZZZ')==105; from datetime import date; d,k=m.computar_cronograma([{'ativo_id':'a01','criticidade':'CRÍTICA','duracao_min':105},{'ativo_id':'a02','criticidade':'ALTA','duracao_min':105},{'ativo_id':'a03','criticidade':'MÉDIA','duracao_min':57}],240,{0,1,2,3,4},date(2026,7,6)); assert len(d)==2 and k['total_os']==3 and k['dias_uteis']==2 and k['data_conclusao']=='2026-07-07' and k['alerta'] is False and k['horas_pessoa']==4.4 or k['horas_pessoa']==4.5, k; print('OK', k)"</automated>
  </verify>
  <done>_est_duracao_min returns the RESEARCH-tabulated minutes for SPLIT/PISO-TETO/JANELA/SELF CONTAINED and the 105 fallback; computar_cronograma packs the 3-asset fixture into exactly 2 working days (a01+a02 on Mon, a03 on Tue), data_conclusao 2026-07-07, alerta False; helpers are pure and importable with no side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /cronograma endpoint — auth, config load, demand query, assembly</name>
  <read_first>
    - .planning/phases/05-cronograma-preventivo/05-RESEARCH.md sections "Endpoint Contract", "Demand Query (SQL)", "equipe_config Consumption", "Criticidade Source", "Security Domain"
    - backend/manutencao.py lines 1181-1207 (GET /equipe/config — copy its row→config + json.loads + _DEFAULT_CONFIG fallback pattern)
    - backend/manutencao.py lines 32-44 (_require_auth)
  </read_first>
  <files>backend/manutencao.py</files>
  <behavior>
    - GET /api/manutencao/cronograma with no Bearer header → 401 (via _require_auth)
    - GET with valid token → 200 and JSON {dias:[{data,dia_semana,itens:[{ativo_id,nome,tipo,criticidade,duracao_min,duracao_h,falta,status}],horas_usadas,horas_disponiveis}], kpis:{total_os,horas_pessoa,dias_uteis,data_conclusao,pct_utilizacao,alerta}}
    - today=2026-07-06 (valid) is honored; today=not-a-date → 422 (date.fromisoformat ValueError caught)
    - categoria=climatizacao filters the demand query via a parameterized WHERE; categoria omitted → all categories
    - One scheduled entry per ativo (dedup by ativo_id keeping the most-urgent falta); assets with no ativo_plano_estado row enter as initial-mobilization with falta=0
  </behavior>
  <action>
    Add @router.get("/cronograma") async def get_cronograma(categoria: Optional[str]=None, today: Optional[str]=None, authorization: str|None=Header(None)) to backend/manutencao.py (append after the equipe endpoints). Per D (GET read-only computado; filtro opcional categoria; _require_auth; today injetável para teste determinístico):

    1. await _require_auth(authorization) first — read-only GET, same gate as GET /equipe/config (no role restriction; visitante allowed, consistent with other authenticated GETs per CONTEXT).

    2. Parse today: if provided, date.fromisoformat(today) inside try/except ValueError → raise HTTPException(422, ...); else None (helper defaults to date.today()). today is the deterministic seam — make it a plain query param exactly as named so the test can pin the run date (this is THE key testability requirement).

    3. Load crew config + capacity by copying the GET /equipe/config loader (row→dict, json.loads dias_semana/turnos with the corrupt-row fallback to _DEFAULT_CONFIG, then _capacidade(config)). cap_dia_min = capacidade["h_dia_total"] × 60; dias_uteis_py = _dias_uteis_set(config["dias_semana"]).

    4. Build the demand list with the parameterized UNION query from RESEARCH "Demand Query (SQL)": recurrent assets from ativo_plano_estado joined to ativos + LEFT JOIN pmoc_* for criticidade, plus initial-mobilization assets (no ativo_plano_estado row, falta=0). Pass categoria as a bound parameter (never string-interpolated — V5/Pitfall, parameterized SQL). Dedup by ativo_id keeping the minimum-falta row (stable tie-break on item id per Pitfall 3). Normalize criticidade via the Task 1 helper; compute duracao_min via _est_duracao_min(tipo); derive a status label (VENCIDA when falta<=0 else PRÓXIMA) and duracao_h = round(duracao_min/60, 2).

    5. Sort the list with the 3-key tuple from Task 1's CRIT_ORDER (criticidade rank → falta asc → ativo_id asc). Python list.sort is stable — build the list then sort; never rely on dict/query iteration order.

    6. Call computar_cronograma(fila, cap_dia_min, dias_uteis_py, today_date) and return {"dias": dias, "kpis": kpis}. FastAPI auto-serializes the dicts — do not hand-build JSON.

    Do not add any INSERT/UPDATE/DELETE — this endpoint must not mutate state.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import backend.manutencao as m; r=[x for x in m.router.routes if getattr(x,'path','')=='/api/manutencao/cronograma']; assert r and 'GET' in r[0].methods, 'route missing'; print('route OK')"</automated>
  </verify>
  <done>The route /api/manutencao/cronograma (GET) is registered on the existing router; it gates on _require_auth, accepts optional categoria and today query params (422 on bad today), loads capacity from equipe_config exactly like GET /equipe/config, runs the parameterized demand UNION + dedup + stable sort, and returns {dias,kpis} with no DB writes.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic tests — test_cronograma, test_cronograma_alerta, requires_auth</name>
  <read_first>
    - .planning/phases/05-cronograma-preventivo/05-RESEARCH.md section "Deterministic Test Dataset" (fixed dataset, crew config, injected today, exact expected dias + KPIs, and the demand>capacity case)
    - tests/test_manutencao.py lines 16-65 (_query, _exec, _seed_user, _seed_sessao, _auth — reuse) and lines 71-130 (_seed_plano pattern for inserting ativos + ativo_plano_estado directly)
  </read_first>
  <files>tests/test_manutencao.py</files>
  <behavior>
    - test_cronograma: seed the 3-asset fixture (a01 SPLIT CRÍTICA falta -5, a02 SPLIT ALTA falta +10, a03 JANELA MÉDIA falta +20) + Mon–Fri 4h crew config; GET cronograma?today=2026-07-06 → 200; assert dias has 2 days, day0.data=="2026-07-06" with itens [a01,a02] in that order, day1.data=="2026-07-07" with [a03]; assert kpis.total_os==3, kpis.dias_uteis==2, kpis.data_conclusao=="2026-07-07", kpis.alerta is False
    - test_cronograma_alerta: seed 3 SELF CONTAINED assets (207 min each) + same 4h/240-min config; GET cronograma?today=2026-07-06 → kpis.alerta is True
    - test_cronograma_requires_auth: GET cronograma with no Authorization header → 401
  </behavior>
  <action>
    Append three tests to tests/test_manutencao.py using the existing sync app_client fixture and the existing _auth/_exec/_query helpers. Per D (dataset fixo + config conhecida → alocação determinística; cobrir demanda>capacidade; today injetável).

    Add a local seed helper that INSERTs the fixed dataset directly via _exec: ativos rows (id, nome, tipo, categoria='climatizacao', uso_atual, criticidade, ativo=1) and matching ativo_plano_estado rows (ativo_id, catalogo_plano_item_id, ultimo_uso, proximo_uso) so that proximo_uso − uso_atual yields the falta values in the RESEARCH fixture. Also UPSERT equipe_config id=1 with dias_semana=["seg","ter","qua","qui","sex"] and turnos summing to 4h (one 4h turno, or two 2h turnos) so h_dia_total=4 → cap_dia_min=240. Mirror the JSON-TEXT serialization the PUT /equipe/config handler uses.

    Drive each request through the client with the _auth(main) header and the today=2026-07-06 query param. Assert the exact dias structure and KPI values from RESEARCH "Expected output". For pct_utilizacao / horas_pessoa, assert against the rounded values the helper produces (compute the expected from the documented formula; if a half-cent rounding ambiguity exists, assert the documented rounded number directly rather than recomputing in the test). The auth test sends no header and asserts status 401.

    Do not weaken existing tests. Run the full module to confirm no regression beyond the known baseline.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py::test_cronograma tests/test_manutencao.py::test_cronograma_alerta tests/test_manutencao.py::test_cronograma_requires_auth -x -q</automated>
  </verify>
  <done>All three new tests pass: test_cronograma asserts the exact 2-day packing + KPIs for the fixed dataset and injected today; test_cronograma_alerta asserts alerta is True for the demand>capacity case; test_cronograma_requires_auth asserts 401. `pytest tests/test_manutencao.py -q` shows no new failures versus the pre-existing baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/manutencao/cronograma | Untrusted query params (categoria, today) + Bearer token cross here |
| endpoint → SQLite | Demand query reads ativos/ativo_plano_estado/pmoc_*/equipe_config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | demand SQL (categoria param) | high | mitigate | Parameterized SQL bind for categoria — never string-interpolated (Task 2 step 4) |
| T-05-02 | Information Disclosure | GET /cronograma | medium | mitigate | _require_auth gate before any data read (Task 2 step 1) |
| T-05-03 | Denial of Service | greedy packing loop | medium | mitigate | cap_dia_min floored ≥1, MAX_JOBS=5000 + MAX_DAYS=365 guards prevent infinite loop on garbage config (Task 1) |
| T-05-04 | Tampering | today query param | low | mitigate | date.fromisoformat parse inside try/except → 422 on malformed input (Task 2 step 2) |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — pure stdlib + existing deps (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `python -m pytest tests/test_manutencao.py -q` — new cronograma tests pass; no regression beyond the documented baseline.
- `python -c "import backend.manutencao"` — module imports cleanly (no syntax/name errors).
- Determinism: re-running `GET /cronograma?today=2026-07-06` against the fixed dataset yields identical dias + KPIs every run (stable sort + injected today).
</verification>

<success_criteria>
- GET /api/manutencao/cronograma is registered, auth-gated, read-only, and returns {dias,kpis} per the RESEARCH contract.
- The fixed dataset + injected today produces the exact RESEARCH-documented 2-day packing and KPIs.
- Demand>capacity yields kpis.alerta == true.
- No new DB tables; no DB writes from the endpoint.
</success_criteria>

<output>
Create `.planning/phases/05-cronograma-preventivo/05-01-SUMMARY.md` when done — include the endpoint contract, the duration/sort/packing constants used, the demand-query shape, and the test dataset so the Wave 2 frontend plan can render against a known JSON shape.
</output>
