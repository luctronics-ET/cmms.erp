---
phase: 08-ajuda-e-documentacao
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/test_docs.py
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "tests/test_docs.py proves ajuda upsert persists and preserves the id; documento create + version upload increments 1→2 with history preserved (autor+data recorded); download returns the exact bytes; list filters by categoria; path-traversal/extension/oversize uploads return 4xx; visualizador write → 403; db.init() twice is idempotent (DOC-01, DOC-02, DOC-03)"
    - "pytest tests/ shows no NEW failures beyond the documented 12-failure baseline"
  artifacts:
    - tests/test_docs.py
  key_links:
    - "test fixtures point DB_PATH and backend.docs._DATA_DOCS at tmp_path so the suite never writes to real data/"
    - "login fixtures obtain operador and visualizador tokens via POST /api/auth/login (same pattern as existing tests/test_*.py)"
---

<objective>
The pytest suite that proves the Plan-01 backend contract for all three requirements: contextual help upsert (DOC-01), versioned document upload/download with preserved history (DOC-02), category/tipo browsing (DOC-03), plus the security rejections (path traversal, bad extension, oversize, visualizador 403) and migration idempotency.

Purpose: This is the executable verification gate for the phase backend. It must run against an isolated tmp DB and tmp documentos dir so it leaves no artifacts and is repeatable, and it must not introduce regressions beyond the known 12-failure baseline.
Output: `tests/test_docs.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ajuda-e-documentacao/08-RESEARCH.md
@.planning/phases/08-ajuda-e-documentacao/08-01-PLAN.md

# Analogs — read before writing
@backend/docs.py
@tests/test_manutencao.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_docs.py covering ajuda, versioning, download, and security</name>
  <files>tests/test_docs.py</files>
  <read_first>
    - tests/test_manutencao.py (or any existing tests/test_*.py) — the asgi-lifespan + httpx AsyncClient fixture, how DB_PATH is isolated, and how login tokens for operador/visualizador roles are obtained
    - 08-RESEARCH.md "Deterministic Test Approach" — the exact fixture shape (monkeypatch DB_PATH, monkeypatch backend.docs._DATA_DOCS to tmp_path, in-memory BytesIO upload) and the test bodies to mirror
    - backend/docs.py (from Plan 01) — the actual endpoint signatures, status codes, and response field names to assert against
  </read_first>
  <action>
    Create tests/test_docs.py using pytest-asyncio + asgi-lifespan + httpx AsyncClient against backend.main.app, following the existing test fixture style. The client fixture MUST monkeypatch DB_PATH to a tmp_path DB AND monkeypatch backend.docs._DATA_DOCS to a tmp_path/documentos dir so no real files are written. Provide token fixtures for an operador (write-capable) and a visualizador (read-only) by seeding those users and calling POST /api/auth/login (mirror the existing tests' approach; if a shared conftest fixture already provides tokens, reuse it).

    Tests to implement:
    (1) test_ajuda_upsert_persists — PUT /api/docs/ajuda/manutencao creates a topic; a second PUT updates titulo/conteudo_md and returns the SAME id (ON CONFLICT DO UPDATE, DOC-01); GET /api/docs/ajuda?chave=manutencao returns the updated content.
    (2) test_documento_create_and_list_by_categoria — POST /api/docs/documentos with categoria=geral; GET /api/docs/documentos?categoria=geral returns it; GET with a different categoria does not (DOC-02/DOC-03).
    (3) test_upload_versao_increments_and_preserves_history — upload a small in-memory pdf (BytesIO) → versao 1; upload again → versao 2; GET /api/docs/documentos/{id} returns both versions with autor and data populated and v1 still present (DOC-02 version control).
    (4) test_download_returns_correct_bytes — download version 1 returns 200 and the exact bytes uploaded for v1 (not v2).
    (5) test_extension_rejected — uploading a .exe (or .sh) returns 4xx (400); the file is NOT written to disk.
    (6) test_oversize_rejected — uploading bytes over MAX_UPLOAD_BYTES returns 413 (use a small monkeypatched cap or a payload just over the boundary; prefer asserting the boundary behavior without allocating 25 MB — patch backend.docs.MAX_UPLOAD_BYTES to a small value for this test).
    (7) test_path_traversal_defense — creating a documento with categoria='../../etc' (or otherwise out-of-whitelist) returns 400, so no traversal path is ever constructed; assert nothing is written outside the tmp documentos dir.
    (8) test_visualizador_write_403 — visualizador token on POST /api/docs/documentos AND on POST .../versoes AND on PUT /api/docs/ajuda/{chave} each returns 403 (RES-05).
    (9) test_migracoes_idempotentes — CoreDB(tmp).init() called twice raises no error and the three tables exist.

    Keep payloads tiny (BytesIO of a few bytes). Do not depend on network or real seed data beyond the login users.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_docs.py -q 2>&1 | tail -25</automated>
  </verify>
  <done>All tests in tests/test_docs.py pass; each of the 9 behaviors above is asserted; the suite writes only under tmp_path.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no regression beyond the 12-failure baseline</name>
  <files>tests/test_docs.py</files>
  <read_first>
    - .planning/STATE.md — note of the pre-existing 12-failure baseline
  </read_first>
  <action>
    Run the full suite and confirm the only failures are the documented pre-existing 12 (or fewer). Do NOT modify unrelated tests to make them pass; if test_docs.py itself reveals a real bug in backend/docs.py from Plan 01, fix the bug in docs.py rather than weakening the test. Record the before/after failure counts in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full-suite failure count is &le; 12 (the baseline) and tests/test_docs.py contributes zero new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → filesystem | Tests must not write to the real data/ tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08T-01 | Tampering | test run polluting real data/documentos | low | mitigate | fixture monkeypatches backend.docs._DATA_DOCS and DB_PATH to tmp_path; assert no writes escape it |
| T-08T-02 | Tampering | weakening a test to force green | medium | mitigate | Task 2 forbids editing unrelated tests; a real bug is fixed in docs.py, not by relaxing the assertion |
</threat_model>

<verification>
- `pytest tests/test_docs.py -q` is fully green.
- `pytest tests/ -q` failure count &le; 12 baseline.
- Security tests (extension, oversize, traversal, 403) all assert 4xx, not 2xx.
- The version-history test confirms v1 survives after v2 upload with autor+data recorded.
</verification>

<success_criteria>
- DOC-01/02/03 behaviors each have a passing test.
- Security rejections proven (400/413/403).
- Migration idempotency proven.
- No new regressions beyond baseline.
</success_criteria>

<output>
Create `.planning/phases/08-ajuda-e-documentacao/08-02-SUMMARY.md` when done.
</output>
