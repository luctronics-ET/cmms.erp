---
phase: 06-residuais-funcionais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db_core.py
  - backend/main.py
  - backend/manutencao.py
  - tests/test_manutencao.py
autonomous: true
requirements: [RES-01, RES-04]
must_haves:
  truths:
    - "A por_tempo plan raises an alert when today >= last_exec_date + interval_days (RES-01)"
    - "por_tempo and por_uso are mutually exclusive per plan — only the plan's tipo_gatilho is evaluated"
    - "Thermal calc reads locais.altura_m when present and falls back safely when NULL/missing (RES-04)"
    - "db.init() run twice does not error (additive idempotent migrations)"
  artifacts:
    - "backend/db_core.py with altura_m column added to locais migration block"
    - "backend/main.py manutencao_vencimentos evaluating por_tempo branch + LocalIn.altura_m + create/update_local persisting altura_m"
    - "backend/manutencao.py _vencimentos_para_ativo evaluating por_tempo branch"
    - "tests/test_manutencao.py with por_tempo-by-date and thermal-NULL-safe tests"
  key_links:
    - "manut_registros.data (MAX by ativo_id) → por_tempo last-execution source"
    - "locais.altura_m column → list_refrigeracao SELECT l.altura_m AS local_altura_m → frontend calcBTU"
---

<objective>
Land the schema foundation and date-based maintenance logic for Phase 6: add the `altura_m` column to `locais` (RES-04), and teach the two vencimento code paths to evaluate `por_tempo` plans by date (RES-01).

Purpose: por_tempo plans (e.g. climatização `{"tipo":"por_tempo","valor":90,"unidade":"dias"}`) are currently silenced by a `por_uso`-only guard; and thermal BTU calc references a `locais.altura_m` column that does not yet exist in the migration block, risking a persistence failure on INSERT/UPDATE.

Output: additive idempotent migration for `locais.altura_m`; a `por_tempo` branch in both vencimento paths; `LocalIn.altura_m` + create/update_local persistence; targeted tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-residuais-funcionais/06-RESEARCH.md

@backend/db_core.py
@backend/main.py
@backend/manutencao.py
@tests/test_manutencao.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add altura_m migration to locais (RES-04)</name>
  <files>backend/db_core.py</files>
  <read_first>backend/db_core.py:38-45 (locais_existing migration block — PRAGMA table_info(locais) then conditional ALTER per missing column, matching the established additive pattern)</read_first>
  <action>Per RES-04 and D-RES-04: in the `locais_existing` migration block in backend/db_core.py, append a new tuple `("altura_m", "ALTER TABLE locais ADD COLUMN altura_m REAL")` to the column-DDL list that is iterated against `locais_existing`. The existing `if col not in locais_existing: await db.execute(ddl)` loop already makes this idempotent (PRAGMA check before ALTER, never DROP — per CLAUDE.md additive-migration rule). Do NOT alter any other migration block in this task.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import ast,sys; src=open('backend/db_core.py').read(); assert 'ALTER TABLE locais ADD COLUMN altura_m REAL' in src, 'altura_m ALTER missing'; ast.parse(src); print('OK db_core altura_m')"</automated>
  </verify>
  <done>backend/db_core.py contains the `altura_m` ALTER inside the locais migration loop; module parses; no DROP introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate por_tempo in both vencimento paths (RES-01)</name>
  <files>backend/main.py, backend/manutencao.py</files>
  <read_first>
    backend/main.py:2529-2583 (manutencao_vencimentos — line 2568 `if f.get("tipo") != "por_uso" or not f.get("valor"): continue` is the silencing guard).
    backend/manutencao.py:65-134 (_vencimentos_para_ativo — line 110 has the identical guard).
    06-RESEARCH.md "RES-01 — Mudança mínima" for the branch shape and "Pitfall 1" for the mandatory mutual-exclusion control flow.
  </read_first>
  <behavior>
    - Given a por_tempo plan item ({"tipo":"por_tempo","valor":90,"unidade":"dias"}) and a manut_registros row dated >= 90 days ago for that ativo_id, the vencimento output includes an alert entry for that item with unidade "dias".
    - Given the same plan but the most recent manut_registros.data is < (90 minus the 15% window) days ago, no alert is emitted for it.
    - Given a por_tempo plan with NO manut_registros rows for the ativo, no alert is emitted (no date base → no trigger) and no exception is raised.
    - Given a por_uso plan, behavior is unchanged (por_tempo branch does not affect it; mutual exclusivity preserved).
  </behavior>
  <action>Per RES-01 and D-RES-01: in BOTH manutencao_vencimentos (main.py guard ~2568) and _vencimentos_para_ativo (manutencao.py guard ~110), replace the blind `por_uso`-only `continue` with a branch that first handles `f.get("tipo") == "por_tempo"`. For por_tempo: read `valor` (skip if falsy); fetch the last execution date via `SELECT MAX(data) AS d FROM manut_registros WHERE ativo_id = ?` bound to the current ativo id (the loop variable is `a["id"]` in manutencao_vencimentos and the `ativo_id` parameter in _vencimentos_para_ativo — use the correct identifier in each file); if no date, `continue` (no base → no alert). Parse the ISO date with `datetime.date.fromisoformat` inside try/except ValueError (continue on parse failure — do NOT hand-roll string comparison). Compute `dt_prox = dt_ultima + timedelta(days=int(valor))` and `falta = (dt_prox - date.today()).days`; emit an alert entry only when `falta <= int(valor) * 0.15` (same 15% window the por_uso path uses), appending a dict to the same output list with keys consistent with existing por_uso entries plus `unidade: "dias"`, `falta`, and `proximo: dt_prox.isoformat()`. The por_tempo branch MUST end with `continue` (or be an explicit elif/else) so a por_tempo plan NEVER falls through into the por_uso valor-division logic — this prevents the division-by-zero in Pitfall 1. Keep the existing `por_uso` guard as the else/fall-through. Import `date` and `timedelta` from datetime at function or module scope as already conventioned in each file.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py -k "por_tempo" -x -q</automated>
  </verify>
  <done>por_tempo plans alert by date in both paths; por_tempo plans with no manut_registros do not alert and do not raise; por_uso plans unchanged; the new pytest -k por_tempo cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist locais.altura_m through LocalIn + CRUD + thermal-NULL-safe test (RES-04)</name>
  <files>backend/main.py, tests/test_manutencao.py</files>
  <read_first>
    backend/main.py:885-895 (LocalIn model — has area_m2, lacks altura_m).
    backend/main.py:1927-1945 (create_local INSERT ~1930 and update_local UPDATE ~1940 — neither references altura_m).
    backend/main.py:1094 (list_refrigeracao already SELECTs `l.altura_m AS local_altura_m`).
    tests/test_manutencao.py:40-65 (_seed_user / _seed_sessao / _auth helpers to model new tests on).
  </read_first>
  <behavior>
    - POST /api/locais with altura_m set persists it; the value round-trips through the locais row.
    - A local with area_m2 NULL and altura_m NULL is creatable and listable via the refrigeração path without raising (frontend fallback `|| 2.7` and `area<=0 → null` already handle display).
  </behavior>
  <action>Per RES-04 and D-RES-04: add `altura_m: Optional[float] = None` to LocalIn (main.py ~885). In create_local (INSERT ~1930) add `altura_m` to the explicit column list and `body.altura_m` to the values tuple. In update_local (UPDATE ~1940) add `altura_m = ?` to the SET clause and `body.altura_m` to its params, mirroring how area_m2 is handled. Do not change the list_refrigeracao SELECT (it already reads l.altura_m). Then add a test in tests/test_manutencao.py (model it on the existing _auth helper) asserting: (a) POST /api/locais with altura_m persists and a subsequent read returns it; (b) a local created with area_m2=None and altura_m=None is listed via the refrigeração endpoint with HTTP 200 and no exception. Keep migrations idempotent: the test suite already recreates the DB via db.init(); ensure the new column exists because Task 1 added the migration.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py -k "altura or termico or thermal or local" -x -q</automated>
  </verify>
  <done>LocalIn carries altura_m; create_local/update_local persist it; thermal-NULL-safe test passes; existing local tests show no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/locais | Untrusted altura_m/area_m2 numeric input crosses into persistence |
| DB schema migration | db.init() mutates schema on boot; must stay additive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | locais altura_m migration | medium | mitigate | PRAGMA-guarded additive ALTER only; never DROP; idempotent on repeat db.init() |
| T-06-02 | Denial of Service | manutencao_vencimentos por_tempo branch | low | mitigate | try/except around date parse; missing/invalid date → continue (no crash, no alert) |
| T-06-03 | Information Disclosure | list_refrigeracao altura_m | low | accept | altura_m is non-sensitive facility geometry; read path unchanged |
</threat_model>

<verification>
- `python -m pytest tests/test_manutencao.py -q` shows the new por_tempo and thermal tests passing.
- Running db.init() twice (test suite does this) raises no "duplicate column" error.
- No pre-existing test regresses beyond the known 14-failure baseline.
</verification>

<success_criteria>
- RES-01: por_tempo alert fires by date in both manutencao_vencimentos and _vencimentos_para_ativo; mutually exclusive with por_uso; no crash when manut_registros empty.
- RES-04: locais.altura_m migrated additively; LocalIn + create/update_local persist it; thermal calc safe when area/altura NULL.
</success_criteria>

<output>
Create `.planning/phases/06-residuais-funcionais/06-01-SUMMARY.md` when done.
</output>
