---
phase: 07-auth-hardening
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/test_manutencao.py
  - tests/test_sync_eventos.py
  - tests/test_import_ata2_climatizacao.py
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "tests/test_manutencao.py has at least one passing test exercising each Phase 1-5 route family: uso, plano-ativo, registro, sobressalentes CRUD+ajuste, equipe membros+config, cronograma"
    - "Any coverage gap found in those route families is filled with a new passing test"
    - "pytest -x on the full suite shows no NEW failures beyond the documented pre-existing set"
    - "The auth tests from 07-01/07-02 plus the manutencao suite all pass"
  artifacts:
    - tests/test_manutencao.py (gap-filling tests, if any gaps found)
    - .planning/phases/07-auth-hardening/07-03-SUMMARY.md (coverage audit table + pre-existing-failure disposition)
  key_links:
    - "QA-01 audit -> route inventory of Phases 1-5 -> per-route test existence check -> gap-fill"
---

<objective>
Audit pytest coverage of every manutencao route imported in Phases 1-5 and fill any gaps,
then run the full suite to confirm zero new regressions on top of the Argon2 / default-
removal changes from 07-01 and 07-02. Attempt only the cheapest of the 14 documented
pre-existing failures; document the rest rather than ballooning the phase. This plan
implements QA-01. It runs last (Wave 3) so the final `pytest -x` gate reflects the
completed auth changes.

Purpose: SEC-01/SEC-02 changed the highest-risk code in the milestone. QA-01 is the
safety net that proves the manutencao routes and the new auth behavior all pass together
and that nothing regressed.

Output: any needed gap-filling tests in tests/test_manutencao.py, optional cheap fixes to
1-2 pre-existing failures, and a coverage-audit + failure-disposition SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auth-hardening/07-CONTEXT.md
@.planning/phases/07-auth-hardening/07-01-SUMMARY.md
@.planning/phases/07-auth-hardening/07-02-SUMMARY.md

@tests/test_manutencao.py
@backend/manutencao.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 1-5 route coverage and fill gaps</name>
  <files>tests/test_manutencao.py</files>
  <read_first>
    - tests/test_manutencao.py (full file — 22 existing test functions; helpers at lines 18-65)
    - backend/manutencao.py (the router — enumerate every @router.get/post/put path)
    - .planning/phases/07-auth-hardening/07-CONTEXT.md QA-01 decisions (route families to cover)
  </read_first>
  <action>
    Build a route inventory from backend/manutencao.py: list every registered path+method.
    Group into the Phase 1-5 families: (1) Registrar Uso — POST/GET uso + vencimentos;
    (2) Plano no Ativo — GET plano-ativo, POST registro; (3) Sobressalentes — list, create,
    edit, atomic ajuste, movimentos; (4) Equipe Tecnica — membros CRUD, config GET/PUT;
    (5) Cronograma — GET cronograma (+ alerta). For each route, check whether at least one
    test in tests/test_manutencao.py exercises it (the file already has 22 tests — most
    families are covered). For any route with NO test, add a focused passing test mirroring
    the existing helper style (_query/_exec/_seed_user/_seed_sessao/_auth, sync app_client
    fixture). Do NOT rewrite passing tests. Record the per-family coverage result (covered /
    gap-filled) in a table you will paste into the SUMMARY.

    If every family already has coverage (likely), add at most the missing edge-route tests
    and note "no gaps" for the rest. Keep new tests fast and deterministic — no sleeps, no
    network.
  </action>
  <verify>
    <automated>python -m pytest tests/test_manutencao.py -q -p no:cacheprovider</automated>
  </verify>
  <done>Every Phase 1-5 route family has at least one passing test in tests/test_manutencao.py; any gap found is filled; tests/test_manutencao.py is green; a coverage table is ready for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Cheap pre-existing-failure triage + full-suite regression gate</name>
  <files>tests/test_sync_eventos.py, tests/test_import_ata2_climatizacao.py</files>
  <read_first>
    - The 14 documented pre-existing failures (baseline): 10 in test_catalogo.py
      (catalogo plano endpoints return 410 / KeyError 'id' — endpoints unimplemented,
      OUT OF SCOPE for this phase), 1 in test_import_ata2_climatizacao.py (FileNotFoundError),
      2 in test_sync.py, 1 in test_sync_eventos.py (sqlite error).
    - tests/test_sync_eventos.py::test_estoque_mov_saida_decrementa_qtd (the sqlite failure)
    - tests/test_import_ata2_climatizacao.py::test_parse_ata2_estrutura (the FileNotFoundError)
  </read_first>
  <action>
    Triage ONLY the cheap, clearly-bounded pre-existing failures. The 10 test_catalogo
    plano failures depend on unimplemented catalogo endpoints (schema-only per CLAUDE.md) —
    DO NOT attempt them; they are out of scope and would balloon the phase. Inspect the
    test_sync_eventos sqlite failure and the test_import_ata2 FileNotFoundError: if either
    is a trivially-bounded fix (a missing fixture file path, a stale column name, a one-line
    SQL/setup correction) and the fix touches only the test or its fixture (not production
    auth code), apply it. If the fix is non-trivial or reaches beyond the test, leave it and
    document why. Do NOT modify backend/main.py or backend auth code in this task.

    Then run the FULL suite and compare against the documented baseline of 14 pre-existing
    failures. The phase passes QA-01 when: (a) all 07-01/07-02 auth tests pass, (b) all
    manutencao tests pass, (c) no NEW failure was introduced by the auth changes, and (d)
    the remaining pre-existing failures are <= 14 and each is named + dispositioned in the
    SUMMARY. Record the final tally and the disposition of every remaining failure.
  </action>
  <verify>
    <automated>python -m pytest tests/test_auth.py tests/test_manutencao.py -q -p no:cacheprovider</automated>
    <automated>python -m pytest tests/ -q -p no:cacheprovider 2>&1 | tail -20</automated>
  </verify>
  <done>tests/test_auth.py and tests/test_manutencao.py are fully green; the full-suite failure count is <= the 14 pre-existing baseline with no new auth-introduced regressions; every remaining failure is named and dispositioned in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> auth + manutencao routes | Tests are the regression boundary proving SEC-01/SEC-02 changes did not break existing behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | undetected auth regression | high | mitigate | Full-suite gate after 07-01/07-02 confirms no new failures; manutencao + auth suites must be fully green |
| T-07-09 | Repudiation | unverified pre-existing failures | low | accept | The 14 documented failures (10 unimplemented catalogo endpoints + 3 sync/import) are out of auth scope; each is named and dispositioned in the SUMMARY rather than silently ignored |
</threat_model>

<verification>
- `python -m pytest tests/test_auth.py tests/test_manutencao.py -q` is fully green.
- Full suite `python -m pytest tests/ -q` shows no new failures vs the 14-failure baseline.
- SUMMARY contains a Phase 1-5 coverage table and a disposition line per remaining failure.
</verification>

<success_criteria>
- Every Phase 1-5 manutencao route family has passing test coverage.
- Auth + manutencao suites are fully green.
- No new regression introduced by the auth changes; remaining failures <= 14 and documented.
</success_criteria>

<output>
Create `.planning/phases/07-auth-hardening/07-03-SUMMARY.md` when done.
</output>
