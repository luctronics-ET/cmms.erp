---
phase: 06-residuais-funcionais
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/db_core.py
  - backend/main.py
  - tools/backfill_local_id.py
  - tests/test_manutencao.py
autonomous: true
requirements: [RES-02, RES-03, RES-05]
must_haves:
  truths:
    - "POST /api/os accepts optional departamento and GET /api/os/{id} returns it (RES-02)"
    - "ativos.local_id exists; GET /api/ativos returns it; idempotent backfill fills only WHERE local_id IS NULL (RES-03)"
    - "A visualizador receives 403 on guarded write routes and 200 on GET routes (RES-05)"
    - "POST /api/os preserves an external-module path (modulo_origem set) so external integrations are NOT silently broken (RES-05 contract note)"
    - "Re-running the backfill script changes nothing the second time (idempotent)"
  artifacts:
    - "backend/db_core.py with ativos.local_id and ordens_servico.departamento additive migrations"
    - "backend/main.py with OSIn.departamento + INSERT, _require_escrita helper applied to write routes"
    - "tools/backfill_local_id.py idempotent backfill script"
    - "tests/test_manutencao.py with departamento-persist and visualizador-403 tests"
  key_links:
    - "_require_auth(authorization) → _require_escrita(user) → 403 when role=='visualizador'"
    - "pmoc_refrigeracao.local_id → ativos.local_id (backfill, only where NULL)"
    - "OSIn.departamento → ordens_servico INSERT → get_os SELECT o.* auto-returns"
---

<objective>
Complete the backend gaps that build on Plan 01's migration foundation: persist `departamento` on the OS (RES-02), add and backfill `ativos.local_id` (RES-03), and enforce the `visualizador` role with 403 on write routes while keeping reads open and external-module OS creation working (RES-05).

Purpose: OS lotação must persist in a dedicated column; non-climatização assets need their `local_id` relinked; and read-only users must be blocked from writes without breaking external-module `POST /api/os` integration.

Output: two additive migrations; OSIn + create_os carrying departamento; an idempotent backfill script; a reusable `_require_escrita` guard applied across write routes with a preserved external-module path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-residuais-funcionais/06-RESEARCH.md
@MODULOS_EXTERNOS.md

@backend/db_core.py
@backend/main.py
@tests/test_manutencao.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive migrations + departamento on OS (RES-02, RES-03 schema)</name>
  <files>backend/db_core.py, backend/main.py, tests/test_manutencao.py</files>
  <read_first>
    backend/db_core.py:27-37 (ativos `existing` migration block) and :54-67 (os_existing block) — both use PRAGMA → `if col not in existing: ALTER`.
    backend/main.py:899-911 (OSIn) and :2046-2064 (create_os INSERT with 13 explicit columns + values tuple; get_os at :2025 already SELECTs o.*).
    06-RESEARCH.md "RES-02 — Mudança mínima" and "RES-03 — Mudança mínima".
  </read_first>
  <behavior>
    - POST /api/os with departamento set returns 201 and the created OS read back via GET /api/os/{id} includes departamento with that value.
    - POST /api/os without departamento still returns 201 and departamento is null (no contract break for existing clients).
    - After migration, GET /api/ativos rows include a local_id key.
  </behavior>
  <action>Per RES-02/RES-03 and D-RES-02/D-RES-03: (1) In db_core.py, add `("departamento", "ALTER TABLE ordens_servico ADD COLUMN departamento TEXT")` to the os_existing column list, and `("local_id", "ALTER TABLE ativos ADD COLUMN local_id INTEGER REFERENCES locais(id)")` to the ativos `existing` column list. Both rely on the existing PRAGMA-guarded `if col not in existing` loop (additive, idempotent, never DROP per CLAUDE.md). (2) In main.py add `departamento: Optional[str] = None` to OSIn (~899). (3) In create_os (INSERT ~2051) add `departamento` to the explicit column list and `body.departamento` to the values tuple — get_os already uses `SELECT o.*` so the response auto-includes it (no SELECT edit). (4) Add a test in tests/test_manutencao.py asserting POST /api/os with departamento persists and round-trips via GET /api/os/{id}, and that omitting departamento still yields 201. Do not change other migration blocks.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py -k "departamento or local_id" -x -q</automated>
  </verify>
  <done>ordens_servico.departamento and ativos.local_id migrated additively; OSIn + create_os persist departamento; GET round-trips it; GET /api/ativos exposes local_id; departamento-persist test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent local_id backfill script (RES-03)</name>
  <files>tools/backfill_local_id.py</files>
  <read_first>
    06-RESEARCH.md "RES-03 — Heurística de backfill" (conservative: copy pmoc_refrigeracao.local_id into ativos.local_id only WHERE ativos.local_id IS NULL; leave ambiguous categories NULL for manual assignment via the ficha).
    backend/db_core.py for the DB path/connection pattern (CoreDB / DB_PATH env) to reuse rather than hand-roll a connection.
  </read_first>
  <action>Per RES-03 and D-RES-03: create tools/backfill_local_id.py that opens the core DB using the same path resolution the app uses (DB_PATH env / data/core.db default, mirroring db_core.py — do not hardcode a different path). Run a single idempotent UPDATE: set `ativos.local_id` from the matching `pmoc_refrigeracao.local_id` ONLY `WHERE ativos.local_id IS NULL` AND a non-null source exists for that ativo. Do NOT touch rows that already have a local_id and do NOT clear any value (never overwrite, never NULL-out). Print a count of rows updated. The script MUST be safe to run repeatedly — the second run updates 0 rows because the first run already filled the NULLs. Leave non-refrigeração categories (transportes/corte/fonoclama) untouched so they stay manually assignable via the existing ficha edit. Guard against the table not existing (skip gracefully if pmoc_refrigeracao absent).</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -c "import ast; ast.parse(open('tools/backfill_local_id.py').read()); print('OK backfill parses')" && python tools/backfill_local_id.py && python tools/backfill_local_id.py</automated>
  </verify>
  <done>tools/backfill_local_id.py runs twice without error; second run reports 0 rows updated; only NULL local_id rows with a pmoc_refrigeracao source are filled; nothing is overwritten or cleared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: visualizador 403 guard on write routes, reads open, external-module path preserved (RES-05)</name>
  <files>backend/main.py, tests/test_manutencao.py</files>
  <read_first>
    backend/main.py:828-839 (_require_auth — returns dict with role) and the canonical guard pattern in manutencao.py:724-726.
    06-RESEARCH.md "RES-05 — Rotas de escrita" table (the 12 write routes) and "Pitfall 3" (must call _require_auth BEFORE _require_escrita).
    backend/main.py:2046-2057 (create_os currently has NO authorization param; body.modulo_origem is the external-module discriminator) and MODULOS_EXTERNOS.md (external modules POST /api/os with modulo_origem).
  </read_first>
  <behavior>
    - A visualizador session calling a guarded write route (e.g. POST /api/ativos, POST /api/estoque/{id}/movimentos, PUT /api/os/{id}/status) receives HTTP 403.
    - The same visualizador session calling a GET route (e.g. GET /api/ativos) receives HTTP 200 (reads stay open).
    - An operador/admin session calling the same write route succeeds (no 403).
    - POST /api/os called with modulo_origem set (external-module path) still succeeds without requiring an interactive user token — the external integration is not broken.
  </behavior>
  <action>Per RES-05 and D-RES-05: add a reusable helper just after _require_auth in main.py: `def _require_escrita(user: dict) -> None:` that raises HTTPException(403, "Visualizadores não têm permissão de escrita") when `user.get("role") == "visualizador"`, mirroring the manutencao.py pattern. Apply `user = await _require_auth(authorization); _require_escrita(user)` (auth FIRST per Pitfall 3) to the write routes listed in RESEARCH: PUT /api/os/{id}/status, POST /api/ativos, PUT /api/ativos/{id}, POST /api/estoque, PUT /api/estoque/{id}, POST /api/estoque/{id}/movimentos, POST /api/locais, PUT /api/locais/{id}, PUT /api/pmoc/refrigeracao/{id}, POST /api/pmoc/refrigeracao/{id}/os-preventiva, POST /api/manutencao/os-preventiva. Each of these gains an `authorization: str | None = Header(None)` parameter where it lacks one. Leave all GET routes untouched (no guard).

    EXTERNAL-MODULE CONTRACT (POST /api/os): per MODULOS_EXTERNOS.md, external modules create OS with `modulo_origem` set and NO interactive user token. Do NOT add an unconditional mandatory auth to POST /api/os that would 401 those callers. Instead: when `body.modulo_origem` is set, allow the request to proceed on the existing external-module path (no _require_auth/_require_escrita requirement). When `body.modulo_origem` is NOT set (an interactive ERP user creating an OS), require auth and apply _require_escrita so a visualizador gets 403. This makes POST /api/os the one route with conditional enforcement; document this branch with a brief inline comment referencing MODULOS_EXTERNOS.md so the contract change is surfaced, not silent. Add a test asserting: visualizador → 403 on a guarded write; visualizador → 200 on GET /api/ativos; and POST /api/os with modulo_origem set still returns 201 without a user token.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python -m pytest tests/test_manutencao.py -k "visualizador or role or escrita or externo or modulo" -x -q</automated>
  </verify>
  <done>_require_escrita applied to the listed write routes; visualizador gets 403 on writes and 200 on GET; non-visualizador writes succeed; POST /api/os with modulo_origem still works token-free; role tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→write routes | Untrusted callers attempt state-changing operations; role must gate them |
| external module→POST /api/os | Trusted-but-tokenless machine path (modulo_origem) must remain open |
| DB schema migration | additive ALTERs on ordens_servico/ativos must stay idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | write routes lacking role guard | high | mitigate | _require_escrita(user) after _require_auth → 403 for visualizador on all listed write routes |
| T-06-05 | Spoofing/DoS | POST /api/os newly requiring auth could 401 external modules | high | mitigate | conditional: modulo_origem-set path stays tokenless; only interactive (no modulo_origem) path requires auth+escrita; branch documented inline referencing MODULOS_EXTERNOS.md |
| T-06-06 | Tampering | local_id backfill overwriting data | medium | mitigate | UPDATE only WHERE local_id IS NULL; never clears/overwrites; idempotent second run = 0 rows |
| T-06-07 | Tampering | departamento/local_id migrations | medium | mitigate | PRAGMA-guarded additive ALTER; never DROP; idempotent |
</threat_model>

<verification>
- `python -m pytest tests/test_manutencao.py -q` shows departamento-persist and visualizador-403 tests passing.
- `python tools/backfill_local_id.py` run twice: second run reports 0 updates.
- POST /api/os with modulo_origem set returns 201 without a Bearer token (external-module path intact).
- No pre-existing test regresses beyond the known 14-failure baseline.
</verification>

<success_criteria>
- RES-02: departamento persists on OS and round-trips via GET; optional field, no contract break.
- RES-03: ativos.local_id migrated; GET /api/ativos returns it; idempotent backfill fills only NULLs.
- RES-05: visualizador blocked (403) on writes, allowed (200) on reads; external-module POST /api/os preserved.
</success_criteria>

<output>
Create `.planning/phases/06-residuais-funcionais/06-02-SUMMARY.md` when done.
</output>
