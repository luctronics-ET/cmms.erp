---
phase: 06-residuais-funcionais
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmasm_erp.html
autonomous: false
requirements: [RES-02, RES-03]
must_haves:
  truths:
    - "Opening a SR from a service context pre-fills ativo_id + item in the SR modal (RES-02 frontend)"
    - "refri171 (and other non-climatizacao assets) can be assigned a local via the existing editable ficha (RES-03 frontend)"
    - "Opening a SR with no context behaves exactly as before (no regression)"
  artifacts:
    - "cmasm_erp.html abrirNovaSR extended to accept optional context and pre-select sr-item-id / sr-qtd"
    - "cmasm_erp.html ficha edit confirmed to expose local_id assignment for non-climatizacao assets"
  key_links:
    - "abrirNovaSR(osId, ctx) -> sr-item-id select pre-fill via onSRItemChange"
    - "ficha edit (PUT /api/pmoc/refrigeracao/{id} with local_id in _ATIVO_EDIT) -> refri171 local assignable"
---

<objective>
Land the two purely-frontend pieces of Phase 6 (file `cmasm_erp.html` only, no backend overlap): pre-fill the SR modal with ativo_id + item when a SR is opened from a service/maintenance context (RES-02), and confirm/wire the editable ficha so non-climatizacao assets like `refri171` can be assigned a `local` from the ERP (RES-03).

Purpose: SR creation from a service should not force re-typing the ativo + item; and assets that were never linked to a `local` need an assignment path in the UI.

Output: `abrirNovaSR` accepts optional context and pre-selects the stock item; the ficha edit confirmed to expose local assignment. This is a checkpointed (human-verify) plan because the deliverable is interactive UI behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-residuais-funcionais/06-RESEARCH.md

@cmasm_erp.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend abrirNovaSR to pre-fill ativo_id + item from context (RES-02)</name>
  <files>cmasm_erp.html</files>
  <read_first>
    cmasm_erp.html:2874-2885 (_osPrefill, setOsPrefill, novaOSComContexto — existing prefill mechanism).
    cmasm_erp.html:5297-5303 (abrirNovaSR(osId) — opens modal-nova-sr, sets _currentOSId, does NOT pre-fill).
    cmasm_erp.html:2192-2253 (modal-nova-sr fields: sr-tipo, sr-item-id select, sr-qtd, sr-unidade) and :3277 (sr-item-id population), :2210 (onSRItemChange onchange hook).
    06-RESEARCH.md "RES-02 — Localizacao exata — frontend SR prefill" (extend abrirNovaSR to accept optional ctx and pre-select sr-item-id / sr-qtd).
  </read_first>
  <action>Per RES-02 and D-RES-02: change `abrirNovaSR(osId)` to `abrirNovaSR(osId, ctx)` with `ctx` optional (default empty object). Preserve all current behavior when ctx is absent — the existing callers at lines ~4991 and ~5111 pass only osId and must keep working unchanged. When `ctx` carries an item reference (e.g. `ctx.itemId`), pre-select that value in the `sr-item-id` select after the select is populated (run its existing populate path first, then set `.value` and invoke the existing `onSRItemChange()` handler so dependent fields like unidade update). When `ctx` carries `ativoId`, store it for the SR submission and/or surface it in the modal so the SR is associated with the asset. When `ctx.qtd` is present, pre-fill `sr-qtd`. Do not introduce a backend call; this is DOM/localStorage-only prefill consistent with the existing `_osPrefill` pattern. Add no new SR field the backend does not accept.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const s=require('fs').readFileSync('cmasm_erp.html','utf8'); const m=s.match(/function abrirNovaSR\s*\(([^)]*)\)/); if(!m) throw new Error('abrirNovaSR not found'); if(!/ctx/.test(m[1])) throw new Error('abrirNovaSR missing ctx param'); if(!/abrirNovaSR\('\$\{o\.id\}'\)/.test(s)) throw new Error('existing single-arg callers must still parse'); console.log('OK abrirNovaSR ctx:', m[1]);"</automated>
  </verify>
  <done>abrirNovaSR accepts optional ctx; with ctx it pre-selects sr-item-id (and sr-qtd / ativo association); existing single-arg callers unchanged; no backend call added.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm refri171 / non-climatizacao local assignable via ficha (RES-03)</name>
  <files>cmasm_erp.html</files>
  <read_first>
    cmasm_erp.html ficha-edit code path that submits to PUT /api/pmoc/refrigeracao/{ativo_id} (the editable asset ficha).
    06-RESEARCH.md "RES-03 — Heuristica de backfill" (refri171 ELETRONICA/BIBLIOTECA: ficha already editable via PUT /api/pmoc/refrigeracao/{id} with local_id in _ATIVO_EDIT whitelist — no special logic needed) and "RES-03 — Mudanca minima" item 3.
  </read_first>
  <action>Per RES-03 and D-RES-03: ensure the ficha edit UI in cmasm_erp.html exposes a `local`/`local_id` field that is editable and submitted for non-climatizacao assets (refri171 included), so a user can assign a local manually. The backend whitelist `_ATIVO_EDIT` already accepts `local_id` (RESEARCH main.py:1114) and Plan 02 guarantees the `ativos.local_id` column exists. If the ficha form already renders and submits a local selector, confirm it is not hidden/disabled for non-climatizacao assets and make the minimal change to surface it for them; if it is missing, add a local select to the ficha edit form bound to `local_id`. Do NOT build a new endpoint — reuse the existing ficha PUT. Keep the change minimal and consistent with the existing form markup and dark-theme design tokens.</action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const s=require('fs').readFileSync('cmasm_erp.html','utf8'); if(!/local_id/.test(s)) throw new Error('ficha local_id binding not present'); console.log('OK ficha references local_id');"</automated>
  </verify>
  <done>The asset ficha edit exposes an editable local_id assignment usable for refri171 / non-climatizacao assets, submitted via the existing ficha PUT; no new endpoint added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify SR prefill + refri171 local assignment</name>
  <files>cmasm_erp.html</files>
  <what-built>
    abrirNovaSR now accepts an optional context that pre-fills the SR modal (item + ativo + qty), and the asset ficha exposes an editable local assignment for non-climatizacao assets such as refri171.
  </what-built>
  <action>
    Pause for human verification of the interactive UI deliverables from Task 1 and Task 2. Serve the app and walk the verifier through the SR prefill flow, the no-context regression check, and the refri171 local assignment, using the steps below.
  </action>
  <how-to-verify>
    1. Serve the app: `cd /home/luc/DEV_ERP/cmasm.erp && python3 -m http.server 8080` then open http://localhost:8080/cmasm_erp.html
    2. Open an OS that came from a service/maintenance context and click "+ SR". Confirm the SR modal opens with the stock item pre-selected (sr-item-id) and, if applicable, the quantity pre-filled — and the SR is tied to the asset.
    3. Open a SR from a plain OS (no service context). Confirm it opens exactly as before with nothing pre-filled (no regression).
    4. Open the ficha of asset `refri171` (ELETRONICA/BIBLIOTECA). Confirm a local selector is present, editable, and saving it persists the local (reload shows the assigned local).
  </how-to-verify>
  <verify>
    <human-check>Verifier confirms SR prefill works from service context, plain SR is unchanged, and refri171 local assignment persists across reload.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe what did not pre-fill / persist.</resume-signal>
  <done>Human approves: SR prefills item+ativo from context, no-context SR unchanged, refri171 local assignment persists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM->SR submit | Pre-filled values must still be validated by the existing SR submit path |
| ficha edit->PUT /api/pmoc/refrigeracao/{id} | local_id assignment crosses to backend; backend whitelist already gates fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | SR prefill ctx | low | accept | prefill only sets existing form fields; backend SR validation unchanged; no new field accepted |
| T-06-09 | Elevation of Privilege | ficha local_id submit | low | mitigate | reuses existing ficha PUT which Plan 02 guards with _require_escrita (visualizador blocked); _ATIVO_EDIT whitelist limits writable columns |
</threat_model>

<verification>
- Node syntax/structure checks for abrirNovaSR ctx param and ficha local_id binding pass.
- Human-verify checkpoint confirms SR prefill, no-context regression-free behavior, and refri171 local assignment persisting.
</verification>

<success_criteria>
- RES-02 (frontend): SR opened from a service context pre-fills item + ativo; plain SR unchanged.
- RES-03 (frontend): refri171 / non-climatizacao assets get an editable, persistable local assignment via the existing ficha.
</success_criteria>

<output>
Create `.planning/phases/06-residuais-funcionais/06-03-SUMMARY.md` when done.
</output>
