---
phase: 02-plano-no-ativo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/schema_manutencao.sql
  - backend/manutencao.py
  - tests/test_manutencao.py
autonomous: true
requirements: [IMP-02]

must_haves:
  truths:
    - "GET /api/manutencao/plano-ativo?ativo_id= returns every plan item applicable to the ativo's tipo, each with status (VENCIDA/URGENTE/PROXIMA/EM_DIA), falta, pct, ultimo_uso, proximo_uso"
    - "POST /api/manutencao/registro writes one manut_registros row AND upserts ativo_plano_estado.proximo_uso for each checked item in a single atomic transaction"
    - "proximo_uso is always uso_no_momento (read from ativos.uso_atual inside the txn) + intervalo — never accumulates; two registros at advancing usage give uso2 + intervalo, not uso1 + 2×intervalo"
    - "responsavel is required (422 if blank); operador is taken from the Bearer token, never from the payload"
    - "pytest tests/test_manutencao.py::test_plano_no_ativo passes; no new regressions vs the 14-failure baseline"
  artifacts:
    - "data/schema_manutencao.sql contains CREATE TABLE IF NOT EXISTS ativo_plano_estado and manut_registros (REAL columns for ultimo_uso/proximo_uso/uso_no_momento)"
    - "backend/manutencao.py exposes GET /plano-ativo and POST /registro on the existing /api/manutencao router"
    - "tests/test_manutencao.py covers GET listing, POST anti-double-count, atomic rollback, and required-responsavel"
  key_links:
    - "POST reads ativos.uso_atual INSIDE the aiosqlite.connect transaction (not from payload) — the anti-double-count invariant depends on this"
    - "ativo_plano_estado upsert uses ON CONFLICT(ativo_id, catalogo_plano_item_id) DO UPDATE (SQLite 3.46 confirmed on target)"
    - "intervalo resolved as catalogo_plano_itens.frequencia OR catalogo_planos.frequencia (item override else plan default); por_tempo items are returned but skip proximo_uso update"
---

<objective>
Backend vertical slice for "Plano no Ativo" (IMP-02): add the two persistence tables, the GET endpoint that merges the catalogo plan with per-ativo state and computes status, and the POST endpoint that atomically records a maintenance event and advances each checked item's proximo_uso. Includes the pytest coverage that proves the anti-double-count invariant and atomicity.

Purpose: persist the legacy `ulm` ("último uso por item") dict in SQLite so maintenance state survives browser clears, is multi-user, and is readable by the Cronograma phase (Phase 5).
Output: `data/schema_manutencao.sql` (2 new tables), `backend/manutencao.py` (2 new endpoints + 1 Pydantic model), `tests/test_manutencao.py` (4 tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plano-no-ativo/02-RESEARCH.md
@.planning/phases/02-plano-no-ativo/02-PATTERNS.md
@.planning/phases/01-registrar-uso/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan (consumed by Plan 02 frontend and Phase 5):

- **Tables** (`data/schema_manutencao.sql`):
  - `ativo_plano_estado(ativo_id, catalogo_plano_item_id, ultimo_uso REAL, proximo_uso REAL, updated_at)` — PK `(ativo_id, catalogo_plano_item_id)`
  - `manut_registros(id, ativo_id, responsavel, operador, data, uso_no_momento REAL, itens_json, observacao, created_at)`
- **Endpoints** (`backend/manutencao.py`, router prefix `/api/manutencao`):
  - `GET /plano-ativo?ativo_id=` → `{ativo_id, ativo_nome, uso_atual, unidade_uso, itens:[{plano_id, plano_nome, item_id, servico_nome, intervalo, unidade, ultimo_uso, proximo_uso, uso_atual, falta, pct, status, por_tempo}]}`
  - `POST /registro` body `{ativo_id, responsavel, itens:[int], observacao?}` → `{ok, uso_no_momento, itens_registrados}`
- **Pydantic model**: `RegistroIn`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ativo_plano_estado + manut_registros tables to schema_manutencao.sql</name>
  <files>data/schema_manutencao.sql</files>
  <read_first>
    - data/schema_manutencao.sql (existing uso_registros block — copy its conventions: PRAGMA foreign_keys, CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS, no DROP/ALTER)
    - 02-RESEARCH.md "New tables" DDL block (lines 130-161) and "Schema addition" code example (lines 529-557) — the authoritative DDL
    - data/schema_catalogo.sql lines 254-269 (catalogo_plano_itens — id is INTEGER PRIMARY KEY AUTOINCREMENT; this is why itens reference INTEGER ids)
  </read_first>
  <action>
    Append two new tables to the END of data/schema_manutencao.sql, after the existing idx_uso_registros_data index. Use CREATE TABLE IF NOT EXISTS only (additive migration per CLAUDE.md — never DROP/ALTER; db_core.py already lists this file in _SCHEMAS so no db_core.py change is needed).

    Table ativo_plano_estado: columns ativo_id (TEXT NOT NULL REFERENCES ativos(id)), catalogo_plano_item_id (INTEGER NOT NULL REFERENCES catalogo_plano_itens(id)), ultimo_uso (REAL NOT NULL DEFAULT 0), proximo_uso (REAL NOT NULL), updated_at (TEXT NOT NULL DEFAULT (datetime('now'))); composite PRIMARY KEY (ativo_id, catalogo_plano_item_id). Add index idx_ape_ativo on (ativo_id). The proximo_uso and ultimo_uso columns MUST be REAL (not TEXT) so numeric comparison with ativos.uso_atual works — TEXT storage causes lexicographic comparison bugs (see RESEARCH Pitfall 3).

    Table manut_registros: id (INTEGER PRIMARY KEY AUTOINCREMENT), ativo_id (TEXT NOT NULL REFERENCES ativos(id)), responsavel (TEXT NOT NULL), operador (TEXT), data (TEXT NOT NULL), uso_no_momento (REAL NOT NULL), itens_json (TEXT NOT NULL), observacao (TEXT), created_at (TEXT NOT NULL DEFAULT (datetime('now'))). Add index idx_mr_ativo on (ativo_id, created_at DESC).

    Add a header comment line referencing Phase 2 / IMP-02 / CONTEXT.md, mirroring the existing Phase 1 header comment style at the top of the file. Per D (Modelo de dados): these tables are the persisted legacy `ulm` dict (estado por item por ativo) plus the execution audit trail.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python3 -c "import sqlite3; c=sqlite3.connect(':memory:'); c.execute('CREATE TABLE ativos(id TEXT PRIMARY KEY)'); c.execute('CREATE TABLE catalogo_plano_itens(id INTEGER PRIMARY KEY)'); sql=open('data/schema_manutencao.sql').read(); c.executescript(sql); cols={r[1]:r[2] for r in c.execute('PRAGMA table_info(ativo_plano_estado)')}; assert cols.get('proximo_uso')=='REAL' and cols.get('ultimo_uso')=='REAL', cols; mr={r[1] for r in c.execute('PRAGMA table_info(manut_registros)')}; assert {'responsavel','operador','uso_no_momento','itens_json'} <= mr, mr; c.executescript(sql); print('OK idempotent + REAL cols')"</automated>
  </verify>
  <done>Both tables created via CREATE TABLE IF NOT EXISTS; proximo_uso/ultimo_uso/uso_no_momento are REAL; running the schema twice raises no error (idempotent); no DROP or ALTER in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add GET /plano-ativo and POST /registro endpoints to manutencao.py</name>
  <files>backend/manutencao.py</files>
  <read_first>
    - backend/manutencao.py FULL (lines 1-234): imports (json, math, sys, date, Optional, aiosqlite already present — no new imports), _db(), _require_auth() (lines 27-44), UsoIn model + field_validator pattern (lines 49-62), _vencimentos_para_ativo() plan resolution (lines 65-134), registrar_uso() atomic transaction (lines 139-200), listar_uso() (lines 203-234)
    - 02-RESEARCH.md "GET Endpoint" (lines 164-233) and "POST Endpoint" (lines 237-345) — the authoritative algorithm, status thresholds, Pydantic model, and atomic transaction code
    - 02-PATTERNS.md "Shared Patterns" (lines 322-376) — auth guard, atomic transaction, ON CONFLICT upsert, status thresholds
  </read_first>
  <action>
    Append two endpoints to backend/manutencao.py after listar_uso (end of file). No new imports needed (json, math, sys, date, Optional, aiosqlite, Header, Query, BaseModel, field_validator all already imported).

    Add Pydantic model RegistroIn: fields ativo_id (str), responsavel (str), itens (list[int]) — these are catalogo_plano_item_id integers, observacao (Optional[str]=None). field_validator on responsavel rejects blank/whitespace ("Responsável é obrigatório", strips). field_validator on itens rejects empty list ("Selecione ao menos um item").

    GET /plano-ativo (query param ativo_id required via Query, authorization via Header, call _require_auth):
    1. Fetch ativo: SELECT id, nome, tipo, uso_atual, unidade_uso FROM ativos WHERE id=? AND ativo=1 (404 if missing).
    2. Resolve plans for the ativo's tipo using the EXACT plano_by_tipo pattern from _vencimentos_para_ativo (parse aplicavel_tipos JSON, append scalar tipo_codigo fallback). Reuse, do not re-derive.
    3. For each plan, fetch items: SELECT i.id, i.seq, i.classe, i.frequencia, s.nome AS servico_nome FROM catalogo_plano_itens i JOIN catalogo_servicos s ON s.id = i.servico_id WHERE i.plano_id=? ORDER BY i.seq.
    4. Load all state rows for this ativo in ONE query: SELECT catalogo_plano_item_id, ultimo_uso, proximo_uso FROM ativo_plano_estado WHERE ativo_id=? → dict keyed by item id.
    5. Per item: resolve raw = it["frequencia"] or plano["frequencia"]. Apply the por_tempo / SEM_FREQ guard BEFORE any numeric coercion — never call float(f["valor"]) until you have proven the item is por_uso with a valid valor:
       - If raw is missing/None OR json.loads(raw) raises (malformed): include the item with por_tempo=true, status="SEM_FREQ", intervalo=None, falta=None, pct=None, proximo_uso=None, ultimo_uso=None; then `continue` to the next item (do NOT fall through to the numeric block). Wrap the parse in try/except continue-style like lines 103-120.
       - After a successful parse, read f.get("tipo"). If f["tipo"] != "por_uso" (e.g. "por_tempo" or any non-por_uso tipo): include the item with por_tempo=true, status="POR_TEMPO", intervalo=None, falta=None, pct=None, proximo_uso=None, ultimo_uso=None; then `continue`. This guard MUST sit BEFORE `iv = float(f["valor"])` so a por_tempo item (which may have no numeric "valor" key) never raises KeyError/TypeError and still appears in the returned itens list.
       - Only for confirmed por_uso items: iv=float(f["valor"]); if iv<=0 treat as SEM_FREQ (guard div-by-zero, continue). If a state row exists use its proximo_uso/ultimo_uso, else proximo_uso=(math.floor(uso/iv)+1)*iv and ultimo_uso=None (GET-only virtual next; see Pitfall 5). Compute falta=proximo_uso-uso; pct=round(min(100,max(0,(uso-(proximo_uso-iv))/iv*100))). Status: falta<=0→VENCIDA, falta<=iv*0.15→URGENTE, falta<=iv*0.30→PROXIMA, else EM_DIA (same constants as _vencimentos_para_ativo and main.py).
    6. Return wrapper {ativo_id, ativo_nome, uso_atual, unidade_uso, itens:[...]} with the per-item shape from the artifacts list above.

    POST /registro (body: RegistroIn, authorization via Header). The ENTIRE algorithm runs inside ONE aiosqlite transaction, in this exact order:
    - user = await _require_auth(authorization); operador = user.get("mat") or user.get("nome") or str(user.get("usuario_id","")).
    - Open ONE atomic transaction: db_path=_db().db_path; async with aiosqlite.connect(db_path) as conn; conn.row_factory=aiosqlite.Row. Follow registrar_uso (lines 154-190) exactly.
    - (a) Read uso_atual (INSIDE txn): SELECT uso_atual FROM ativos WHERE id=? AND ativo=1; 404 if missing; uso_no_momento=float(row["uso_atual"] or 0.0). Read uso_atual ONLY from the DB here, NEVER from the payload — this is the anti-double-count invariant.
    - (b) Build itens_validos = list of {item_id, iv}: for each item_id in body.itens, SELECT i.id, i.frequencia, p.frequencia AS plano_freq FROM catalogo_plano_itens i JOIN catalogo_planos p ON p.id=i.plano_id WHERE i.id=?; 422 if item not found. Resolve iv = item.frequencia override else plano.frequencia default: raw = i.frequencia or plano_freq; if raw is None/blank → SKIP this item (do not append to itens_validos — por_tempo/no-interval items are recorded in the audit row but get no estado upsert). Parse JSON: if f.get("tipo") != "por_uso" → SKIP (por_tempo has no uso threshold). For por_uso: iv=float(f["valor"]) (422 on malformed parse); if iv<=0 → SKIP. Only append {item_id, iv} for confirmed por_uso items with iv>0. itens_validos may be shorter than body.itens (skipped items are intentional, not errors).
    - (c) Insert audit row: INSERT INTO manut_registros (ativo_id, responsavel, operador, data, uso_no_momento, itens_json, observacao) VALUES (...) with data=date.today().isoformat() and itens_json=json.dumps(body.itens) (logs ALL requested items, including skipped por_tempo ones).
    - (d) Upsert estado per item in itens_validos ONLY: novo_proximo = uso_no_momento + item["iv"] (ALWAYS uso_no_momento + iv — never previous proximo_uso + iv, never NULL: uso_no_momento is a float and iv>0, so novo_proximo is always a positive REAL). Upsert: INSERT INTO ativo_plano_estado (ativo_id, catalogo_plano_item_id, ultimo_uso, proximo_uso, updated_at) VALUES (?,?,?,?,datetime('now')) ON CONFLICT(ativo_id, catalogo_plano_item_id) DO UPDATE SET ultimo_uso=excluded.ultimo_uso, proximo_uso=excluded.proximo_uso, updated_at=excluded.updated_at, with params (body.ativo_id, item["item_id"], uso_no_momento, novo_proximo). proximo_uso is declared NOT NULL in the schema, so an accidental NULL would raise — the iv>0 filter in (b) guarantees this never happens.
    - (e) Single await conn.commit(). Raise HTTPException before commit on any error so the transaction rolls back (no partial manut_registros row, no partial estado upsert).
    - Return {ok: True, uso_no_momento, itens_registrados: len(itens_validos)} with status_code 201.

    Use only parameterized SQL (tuple params), never f-strings in SQL — matches T-01-01 mitigation from Phase 1.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python3 -c "import ast,sys; t=ast.parse(open('backend/manutencao.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef))}; src=open('backend/manutencao.py').read(); assert 'RegistroIn' in names, 'RegistroIn missing'; assert '/plano-ativo' in src and '/registro' in src, 'routes missing'; assert 'ON CONFLICT' in src, 'upsert missing'; assert 'uso_no_momento' in src; print('OK structure')" && python3 -c "import importlib,backend.manutencao as m; importlib.reload(m); print('OK imports clean')"</automated>
  </verify>
  <done>RegistroIn model exists with responsavel + itens validators; GET /plano-ativo and POST /registro routes registered on the /api/manutencao router; POST uses a single aiosqlite.connect block with ON CONFLICT upsert and reads uso_atual inside the txn; module imports without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests/test_manutencao.py (GET listing, anti-double-count, atomicity, required responsavel)</name>
  <files>tests/test_manutencao.py</files>
  <behavior>
    - test_plano_no_ativo (anti-double-count, the invariant must be PROVEN, not accidentally idempotent — so usage MUST advance between the two POSTs via an explicit UPDATE):
        1. Seed ativo with uso_atual=1000 (tipo matching a seeded plano) + catalogo_plano + 1 por_uso item (iv=250).
        2. GET /plano-ativo returns the item with status/falta/pct fields.
        3. POST /registro itens=[item_id] (uso is 1000) → assert ativo_plano_estado.proximo_uso == 1000 + 250 == 1250.
        4. EXPLICIT step — _exec("UPDATE ativos SET uso_atual=1100 WHERE id=?", (ativo_id,)) to advance the meter by 100 BETWEEN the two POSTs. Without this UPDATE the test would be trivially idempotent (both POSTs at uso=1000 give 1250) and would NOT prove the invariant.
        5. POST /registro itens=[item_id] again → assert proximo_uso == 1100 + 250 == 1350 (NOT 1500, NOT 1000+2×250). This proves proximo_uso = uso_at_second_registration + iv with no accumulation across two advancing registros.
        6. Atomicity assertion for the happy path: assert exactly 2 rows now exist in manut_registros for the ativo (one audit row written per successful POST inside its own committed txn).
    - test_registro_exige_responsavel: POST /registro with responsavel="" → HTTP 422 (RegistroIn.resp_nao_vazio validator); also POST with the responsavel key omitted entirely → 422. Assert zero manut_registros rows were written (a rejected payload must leave no audit row).
    - test_registro_atomico: POST /registro with itens=[<nonexistent_item_id>] → 422; assert zero rows in manut_registros AND zero rows in ativo_plano_estado for the ativo (the HTTPException raised before commit must roll back the whole txn — no partial audit row, no partial estado upsert; the write is atomic).
    - test_plano_ativo_requires_auth: GET /plano-ativo without Bearer token → 401.
  </behavior>
  <read_first>
    - tests/test_catalogo.py lines 1-66 (_query, _exec, _seed_user, _seed_sessao, _auth helpers — copy these verbatim) and the catalogo_planos/catalogo_plano_itens/catalogo_servicos insert shapes used elsewhere in that file
    - tests/conftest.py (app_client fixture returns (client, main); backend.manutencao already in the reload list — no conftest change)
    - data/schema_catalogo.sql lines 239-269 (catalogo_planos columns incl. aplicavel_tipos, tipo_codigo, frequencia, ativo; catalogo_plano_itens columns)
    - 02-RESEARCH.md "Test: anti-double-count" (lines 609-642) — the canonical assertions
  </read_first>
  <action>
    Create tests/test_manutencao.py. Copy _query, _exec, _seed_user, _seed_sessao, _auth from test_catalogo.py verbatim (sync sqlite3 helpers + app_client fixture pattern). The fixture is app_client → (client, main).

    Add a seed helper that inserts: one ativo (id, nome, tipo='AC_SPLIT', uso_atual=1000, unidade_uso='h', ativo=1) using _exec with INSERT OR IGNORE; one catalogo_servicos row; one catalogo_planos row (ativo=1, tipo_codigo='AC_SPLIT' or aplicavel_tipos='["AC_SPLIT"]', frequencia='{"tipo":"por_uso","valor":250,"unidade":"h"}'); one catalogo_plano_itens row (plano_id, servico_id, seq=0, classe='prev', frequencia=NULL so it inherits the plan default). Capture the autoincrement item id via _query SELECT id FROM catalogo_plano_itens.

    Write the four tests above. For the anti-double-count test, name the primary test exactly `test_plano_no_ativo` (the ROADMAP success criterion references `pytest tests/test_manutencao.py::test_plano_no_ativo`). The explicit `_exec("UPDATE ativos SET uso_atual=1100 WHERE id=?", (ativo_id,))` between the two POSTs is MANDATORY — it is what makes the invariant provable rather than trivially idempotent. Read proximo_uso back via _query after each POST. For test_registro_atomico, _query both manut_registros AND ativo_plano_estado after the bad POST and assert both are empty (proves the rollback is atomic across both tables). For test_registro_exige_responsavel, send responsavel="" in one assertion and omit the key entirely in another, asserting 422 for each and zero manut_registros rows.

    Note: this file name (tests/test_manutencao.py) is distinct from the existing tests/test_manutencao_smoke.py — do not modify the smoke file.

    NOTE on the verify gate below: it negative-greps the test FILE for the literal `body.uso_atual` to prove the test never sends uso_atual in the POST payload (the anti-double-count invariant requires the server to read it from the DB). Do not write that token anywhere in this test file, including comments.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python3 -m pytest tests/test_manutencao.py -x -q 2>&1 | tail -20 && grep -v '^[[:space:]]*#' tests/test_manutencao.py | grep -c 'body.uso_atual' | grep -qx 0 && echo "OK no payload uso_atual in test"</automated>
  </verify>
  <done>tests/test_manutencao.py passes 4/4; test_plano_no_ativo explicitly UPDATEs ativos.uso_atual to 1100 between the two POSTs and proves proximo_uso = uso_at_second_registration + intervalo (1250 then 1350, not 1500); 422 on blank AND missing responsavel with zero audit rows written; test_registro_atomico proves rollback leaves zero rows in BOTH manut_registros and ativo_plano_estado on a bad item (atomic write); 401 without token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/manutencao | Untrusted JSON payload (ativo_id, responsavel, itens) crosses here; Bearer token authenticates |
| handler → SQLite | Parameterized SQL only; multi-table write must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | SQL in GET/POST handlers | high | mitigate | All SQL parameterized via tuple params; never f-string interpolation of ativo_id/item_id |
| T-02-02 | Tampering | proximo_uso from client | high | mitigate | uso_no_momento read from ativos.uso_atual INSIDE the txn; payload uso_atual ignored (anti-double-count) |
| T-02-03 | Spoofing | operador field | medium | mitigate | operador derived from Bearer token (_require_auth), never from payload |
| T-02-04 | Repudiation | maintenance audit trail | medium | mitigate | manut_registros persists responsavel, operador, data, uso_no_momento, itens_json per event |
| T-02-05 | DoS / Integrity | partial multi-table write | high | mitigate | Single aiosqlite.connect block + one commit; HTTPException before commit forces rollback |
| T-02-06 | Information Disclosure | unauth read of plan state | low | mitigate | GET /plano-ativo calls _require_auth (401 without valid token) |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed this phase (aiosqlite/fastapi/pydantic already present per RESEARCH Standard Stack) — no legitimacy gate needed |
</threat_model>

<verification>
- `python3 -m pytest tests/test_manutencao.py -x -q` → 4 passed
- `python3 -m pytest tests/ -q` → no NEW failures beyond the documented 14-failure baseline (test_catalogo auth, test_sync, test_sync_eventos pre-existing per Phase 1 SUMMARY)
- `data/schema_manutencao.sql` runs twice idempotently; ativo_plano_estado.proximo_uso is REAL
- `backend/manutencao.py` imports cleanly; both routes present on the router
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 2 satisfied: POST writes manut_registros + updates ativo_plano_estado.proximo_uso per checked item in one transaction
- ROADMAP Phase 2 criterion 4 satisfied: test_plano_no_ativo green; proximo_uso after two consecutive registros = uso_no_momento + 2×intervalo
- GET returns full plan with VENCIDA/URGENTE/PROXIMA/EM_DIA status, falta, pct per item (backs criterion 1, rendered by Plan 02)
</success_criteria>

<output>
Create `.planning/phases/02-plano-no-ativo/02-01-SUMMARY.md` when done
</output>