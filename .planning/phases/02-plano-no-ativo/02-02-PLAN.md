---
phase: 02-plano-no-ativo
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - assets/erp-manutencao.js
autonomous: false
requirements: [IMP-02]

must_haves:
  truths:
    - "Opening an ativo's Manutenção sub-tab fetches GET /api/manutencao/plano-ativo and renders each plan item with a checkbox, colored status badge (VENCIDA/URGENTE/PROXIMA/EM_DIA), progress bar, and 'faltam X h' text"
    - "Selecting items + a responsável + clicking 'Registrar Manutenção' POSTs to /api/manutencao/registro with Bearer token, then reloads the sub-tab so statuses update (VENCIDA disappears for a just-executed item)"
    - "All server-supplied text (servico_nome, plano_nome, unidade) is inserted via el()/textContent — never via innerHTML — so a malicious service name cannot inject markup"
    - "When the backend has no plano for the ativo's tipo (empty itens), the sub-tab shows a clear empty-state message instead of erroring"
  artifacts:
    - "assets/erp-manutencao.js — the 'manut' sub-tab in openAtivoDrawer is API-backed (renderSubManutAPI async renderer using subBody.replaceChildren)"
    - "assets/erp-manutencao.js — regManut handler replaced with an async fetch POST to /api/manutencao/registro"
  key_links:
    - "The manut sub-tab renderer must be async and write into subBody via replaceChildren (renderSub uses innerHTML for the other tabs; the manut tab takes the async DOM path instead of returning an HTML string)"
    - "Checkbox IDs/classes use the _mn- prefix (not the legacy _mc-/_md-mc) to avoid collision with the old localStorage handler"
    - "Bearer token from localStorage('xcmasm_token'); reuse the ruAuthHeaders()/apiUrl() pattern already used by the registrar-uso tab"
---

<objective>
Frontend vertical slice for "Plano no Ativo" (IMP-02): wire the existing Manutenção sub-tab of the asset drawer (openAtivoDrawer) to the backend endpoints created in Plan 01. The technician sees the real plan items from the catalogo (resolved by tipo), with live status/progress from ativo_plano_estado, checks executed items, picks a responsável, and registers — the list then reloads with updated statuses.

Purpose: replace the localStorage-only `ulm` checklist with the persisted, multi-user backend state, using safe DOM construction (no innerHTML of server data) as enforced since Phase 1.
Output: `assets/erp-manutencao.js` — API-backed `manut` sub-tab renderer + async `regManut` POST handler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plano-no-ativo/02-RESEARCH.md
@.planning/phases/02-plano-no-ativo/02-PATTERNS.md
@.planning/phases/02-plano-no-ativo/02-01-PLAN.md
</context>

<artifacts_this_phase_produces>
- **JS function** `renderSubManutAPI(subBody, ativo)` (or equivalent inline async path) in `assets/erp-manutencao.js` — async; fetches GET /api/manutencao/plano-ativo and builds the checklist via `el()`.
- **Modified** `window._manutD.regManut` — now an async handler POSTing to /api/manutencao/registro.
- Consumes the endpoints from Plan 02-01 (GET /plano-ativo response shape and POST /registro contract).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API-back the Manutenção sub-tab renderer (el()-safe, fetch GET /plano-ativo)</name>
  <files>assets/erp-manutencao.js</files>
  <read_first>
    - assets/erp-manutencao.js lines 1721-2015 (openAtivoDrawer): the _manutD bridge (1729-1810), legacy subManut() (1891-1923), SUBS map (1984), renderSub() which uses subBody.innerHTML (1993-1995), refreshTabBar/modal wiring (1997-2014)
    - assets/erp-manutencao.js lines 1284-1485 (the 'registrar-uso' tab): the APPROVED current pattern — el() construction, apiUrl(), ruAuthHeaders() (defined ~1294), fetch + res.ok handling, replaceChildren, toast()
    - assets/erp-manutencao.js lines 324-327 (apiUrl) and the badgeHtml + SC color map (~1714) and window.engine.badge usage (~527)
    - 02-RESEARCH.md "Frontend: Where and How to Insert" (lines 371-425) and the el()-safe item row example (lines 575-607)
    - 02-PATTERNS.md "assets/erp-manutencao.js — plano checklist section" (lines 205-266)
  </read_first>
  <action>
    Make the Manutenção sub-tab render from the backend instead of the localStorage mock, using safe DOM. The other sub-tabs (status/uso/hist/comb) stay exactly as-is — only the `manut` tab changes.

    Because renderSub() (line 1993) sets subBody.innerHTML = SUBS[activeSub]?.() (a synchronous string path), the manut tab must take an async DOM path instead. Implement an async renderer renderSubManutAPI(subBody, ativo) that:
    1. Immediately shows a loading line: subBody.replaceChildren(el('div', {style:{padding:'16px', color:'var(--ink-3)'}}, 'Carregando plano...')).
    2. Fetches GET apiUrl('/api/manutencao/plano-ativo?ativo_id=' + encodeURIComponent(ativo.id)) with headers from the same Bearer pattern as the registrar-uso tab (Authorization: 'Bearer ' + (localStorage.getItem('xcmasm_token')||'')). On !res.ok: replaceChildren with an el() error message using err.detail; return.
    3. On success, parse {itens, uso_atual, unidade_uso}. If itens is empty, replaceChildren with an el() empty-state: 'Sem plano de manutenção para o tipo deste ativo.' Return.
    4. Build the checklist with el() ONLY. For each item, render a label row containing: a checkbox (el('input',{type:'checkbox', id:'_mn-'+item.item_id, class:'_mn-cb', value:String(item.item_id)})), the servico_nome via a node whose .textContent = item.servico_nome (NEVER innerHTML), a detail line .textContent = `A cada ${item.intervalo} ${item.unidade||unidade_uso||'h'} · faltam ${Math.max(0,item.falta).toFixed(0)} ${item.unidade||'h'}`, a progress bar (outer el + inner el with style.width = item.pct + '%'), and a status badge via window.engine.badge(item.status, COLOR[item.status]) where COLOR maps VENCIDA→'red', URGENTE→'amber', PROXIMA→'blue', EM_DIA→'green' (and POR_TEMPO/SEM_FREQ → a neutral badge, checkbox still allowed). Map status→bar color the same way. Use the _mn- prefix for ALL ids and the _mn-cb class for checkboxes (Pitfall 7 — do not reuse _mc-/_md-mc).
    5. Append the responsável control + Registrar button below the list: a <select id='_mn-resp'> populated with the same static option list the legacy subManut uses (Luciano Ferreira / Carlos Silva / João Mendes / Pedro Santos / Maria Oliveira) — keep static for now; wired to /api/usuarios in Phase 4 per RESEARCH Open Question 1. Add a 'Registrar Manutenção' button (green) whose onclick calls the async regManut handler (Task 2). Store the resp select and subBody in closure scope so regManut can read them without a global getElementById.
    6. Route the manut tab to this async renderer: in renderSub() (line 1993-1995), special-case activeSub==='manut' to call renderSubManutAPI(subBody, ativo) (and NOT set innerHTML); all other tabs keep the existing innerHTML path. The SUBS map stays for the other tabs.

    Do NOT delete the legacy subManut() function body wholesale if other code references it — but it must no longer drive the manut tab. Prefer leaving it unused or replacing its callsite; do not introduce innerHTML of server data anywhere in the new path.

    NOTE on the verify gate: it negative-greps the NEW renderer region for `.innerHTML` applied to server fields. Build the entire checklist with el()/textContent. The literal token the gate checks for must not appear in the new renderer.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python3 -m pytest tests/test_manutencao_smoke.py -x -q 2>&1 | tail -15 && grep -q "plano-ativo" assets/erp-manutencao.js && grep -q "_mn-cb" assets/erp-manutencao.js && echo "OK plano-ativo wired + _mn- prefix"</automated>
  </verify>
  <done>The Manutenção sub-tab fetches GET /api/manutencao/plano-ativo and renders items with el()-built checkboxes (_mn- prefix), status badges, progress bars and 'faltam X' text; empty/err states handled; smoke test (brace-balance + asset includes) passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace regManut with async POST to /api/manutencao/registro + reload</name>
  <files>assets/erp-manutencao.js</files>
  <read_first>
    - assets/erp-manutencao.js lines 1755-1794 (the current localStorage regManut closure inside window._manutD) — this is what gets replaced
    - assets/erp-manutencao.js lines 1437-1483 (registrar-uso POST: fetch + res.ok + err.detail + toast + reload-recentes — the canonical async submit pattern)
    - 02-RESEARCH.md "regManut replacement" (lines 393-418)
  </read_first>
  <action>
    Replace the body of window._manutD.regManut (lines 1755-1794) with an async handler that talks to the backend instead of localStorage:
    1. Read selected item ids from the new checklist: [...subBody.querySelectorAll('._mn-cb:checked')].map(cb => parseInt(cb.value, 10)). Read responsável from the _mn-resp select stored in closure scope.
    2. Validate client-side (advisory): if no items selected → toast('Selecione ao menos um serviço','amber'); return. If no responsável → toast('Informe o responsável','amber'); return.
    3. POST apiUrl('/api/manutencao/registro') with method POST, headers {'Content-Type':'application/json','Authorization':'Bearer '+(localStorage.getItem('xcmasm_token')||'')}, body JSON.stringify({ativo_id: ativo.id, responsavel: resp, itens: sels}). Wrap in try/catch.
    4. On !res.ok: const e = await res.json().catch(()=>({})); toast('Erro: '+(e.detail||('HTTP '+res.status)),'red'); return.
    5. On success: toast('Manutenção registrada','green'); then reload the checklist by re-running the async manut renderer (activeSub='manut'; renderSubManutAPI(subBody, ativo)) so statuses refresh and VENCIDA clears for just-executed items. Do NOT do a full page reload.
    6. On network error (catch): toast('Falha de rede: '+err.message,'red').

    Keep operador OUT of the payload — the backend derives it from the Bearer token (do not add operador to the JSON body).

    The legacy regManut wrote to hist.ulm/hist.manut in localStorage and called saveHist/refresh; remove that logic from this handler (the backend is now the source of truth). Leave the other _manutD methods (regUso, delReg, delManut) untouched.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && python3 -m pytest tests/test_manutencao_smoke.py -x -q 2>&1 | tail -15 && grep -q "api/manutencao/registro" assets/erp-manutencao.js && grep -vqE "operador.*JSON.stringify|JSON.stringify.*operador" <(grep -n "api/manutencao/registro" -A6 assets/erp-manutencao.js) && echo "OK registro POST present, no operador in body"</automated>
  </verify>
  <done>regManut POSTs {ativo_id, responsavel, itens} to /api/manutencao/registro with Bearer token, shows success/error toasts, and reloads the manut sub-tab on success; operador is NOT in the payload; smoke test passes (JS braces balanced).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — plano-no-ativo render, register, reload, persist</name>
  <action>Human-only verification of the rendered Manutenção sub-tab (see how-to-verify steps). No code changes.</action>
  <what-built>The asset drawer's Manutenção sub-tab now reads the real maintenance plan from the backend (GET /api/manutencao/plano-ativo): each plan item shows a checkbox, a colored status badge (VENCIDA/URGENTE/PROXIMA/EM_DIA), a progress bar and 'faltam X h'. Checking items + choosing a responsável + clicking 'Registrar Manutenção' POSTs to /api/manutencao/registro and reloads the list with updated statuses.</what-built>
  <how-to-verify>
    1. Start the backend: `uvicorn backend.main:app --port 8010 --reload` (seed first if needed: `python tools/seed_ativos.py`).
    2. Open http://localhost:8010 and log in (mat + senha 1234).
    3. Go to Manutenção, open an ativo whose tipo has a catalogo plano (e.g. a climatização AC_SPLIT). Click the 🔧 Manutenção sub-tab.
    4. Confirm: the plan items load from the server (not the old mock), each with a checkbox, a status badge, a progress bar, and a 'faltam X h' line.
    5. Check one or more items, pick a responsável, click 'Registrar Manutenção'. Confirm the green toast and that the list reloads — a just-executed item's status moves away from VENCIDA toward EM_DIA, and 'faltam X' increases.
    6. Re-open the same ativo: the updated statuses persist (state came from the DB, not localStorage).
    7. Negative check: open an ativo whose tipo has no plano — confirm the empty-state message appears instead of an error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → DOM | GET /plano-ativo returns server-controlled strings (servico_nome, plano_nome); rendered into the drawer |
| browser → /api/manutencao/registro | User selection + responsável POSTed with Bearer token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering (XSS) | manut sub-tab rendering | high | mitigate | All server text inserted via el()/textContent; no innerHTML of server data (Pitfall 6) |
| T-02-08 | Spoofing | operador attribution | medium | mitigate | operador never sent from client; backend derives it from the Bearer token |
| T-02-09 | Information Disclosure | token in request | low | accept | Bearer token from localStorage sent over the same origin as existing tabs — no new exposure |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages — vanilla JS, no build step (CLAUDE.md) |
</threat_model>

<verification>
- `python3 -m pytest tests/test_manutencao_smoke.py -x -q` → JS brace-balance + asset-include checks pass (no syntax break introduced)
- `grep "plano-ativo" assets/erp-manutencao.js` and `grep "api/manutencao/registro"` both match
- Human checkpoint confirms render + register + reload + persistence + empty-state
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 1 satisfied: technician sees plan items with checkbox, status, progress bar, 'faltam X h'
- ROADMAP Phase 2 criterion 3 satisfied: on re-open, item statuses reflect the updated proximo_uso (VENCIDA does not reappear for a just-executed item)
- No innerHTML of server data anywhere in the new manut renderer (safe DOM, Phase 1 convention upheld)
</success_criteria>

<output>
Create `.planning/phases/02-plano-no-ativo/02-02-SUMMARY.md` when done
</output>
