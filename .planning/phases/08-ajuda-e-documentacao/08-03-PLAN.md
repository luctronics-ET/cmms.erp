---
phase: 08-ajuda-e-documentacao
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - assets/erp-docs.js
  - cmasm_erp.html
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "A 'Documentos' sidebar page lists documents by categoria + tipo with the latest version, badges per tipo, and offers +Novo documento, Upload de versão (file input → multipart POST), version history, and download (DOC-02, DOC-03)"
    - "A contextual help '?' button in .sb-tools opens a drawer that fetches and SAFELY renders the current page's ajuda topic; non-visitante users get an edit mode (textarea → PUT) (DOC-01)"
    - "Help markdown is rendered without innerHTML of untrusted content — via textContent/createTextNode DOM building only (XSS defense)"
  artifacts:
    - assets/erp-docs.js
  key_links:
    - "page-documentos div + 'documentos' nav item wired to showPage('documentos') in cmasm_erp.html"
    - "help '?' button added to .sb-tools (line ~774) opens the drawer for the active page's chave"
    - "erp-docs.js loaded via a deferred <script> tag and all fetches send the Bearer token"
---

<objective>
Frontend slice for Ajuda & Documentação: a top-level "Documentos" page (browse the versioned repository by categoria + tipo, upload new versions, view history, download — DOC-02/DOC-03) plus a contextual help "?" drawer in the ERP shell that shows and lets managers edit the current page's help topic (DOC-01), with a hand-rolled SAFE markdown renderer (no innerHTML of untrusted content).

Purpose: Delivers the user-facing capability for the phase against the Plan-01 endpoints. This plan is NOT autonomous — it ends with a human-verify checkpoint because the value is visual/interactive (drawer behavior, upload flow, badges, safe rendering) and cannot be fully proven by an automated command.
Output: `assets/erp-docs.js`, plus additive edits to `cmasm_erp.html` (nav item, page container, help button, script tag).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ajuda-e-documentacao/08-CONTEXT.md
@.planning/phases/08-ajuda-e-documentacao/08-RESEARCH.md
@.planning/phases/08-ajuda-e-documentacao/08-01-PLAN.md

# Analogs — read before writing
@assets/erp-manutencao.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build assets/erp-docs.js — Documentos page module + help drawer + safe markdown renderer</name>
  <files>assets/erp-docs.js</files>
  <read_first>
    - assets/erp-manutencao.js — the IIFE + 'use strict' structure, window.engine.utils.el usage, how a page root element is located and populated, the API base resolution (window.XCMASM_API_BASE) and Bearer-token fetch pattern, TAB/badge rendering style
    - 08-RESEARCH.md "Safe Markdown Rendering" — the renderMdSafe + _applyInline functions to port verbatim (textContent/createTextNode only)
    - 08-RESEARCH.md Pattern 7 — el() is reachable via window.engine.utils.el; copy a local el() fallback inside the IIFE in case engine is unavailable
  </read_first>
  <action>
    Create assets/erp-docs.js as a single IIFE with 'use strict'. Expose a small window.erpDocs API object. Inside, resolve el from window.engine.utils.el with a local fallback copy (per RESEARCH Pattern 7). Resolve the API base the same way erp-manutencao.js does and build an authFetch helper that always sends Authorization: Bearer <token from localStorage 'xcmasm_token'>.

    DOCUMENTOS PAGE (DOC-02, DOC-03): expose a render/mount function that populates the #page-documentos container. Provide a categoria selector (the 7 PMOC categories + geral) and a tipo filter (modelo|formulario|guia|norma — the tipo filter is how DOC-03 norms/guias are browsed by category). Fetch GET /api/docs/documentos?categoria=&tipo= and render a list/table of documents showing titulo, tipo (as a colored badge per tipo), and the latest version. Actions: "+ Novo documento" (form → POST /api/docs/documentos), "Upload de versão" (an <input type=file> whose change builds FormData and POSTs to /api/docs/documentos/{id}/versoes as multipart), "Histórico" (GET /api/docs/documentos/{id} → list versions with versao/autor/data), and "Baixar" per version (navigate/fetch GET /api/docs/documentos/{id}/versoes/{versao}/download with the Bearer header, then trigger a browser download of the blob using the response filename). Build all DOM via el()/textContent — no innerHTML of server data.

    HELP DRAWER (DOC-01): expose openAjuda(chave) which fetches GET /api/docs/ajuda?chave=<chave> and renders the topic into a fixed-position drawer using the ported renderMdSafe(conteudo_md, container) — NEVER assign innerHTML of conteudo_md. If the user role is not visitante (read role from the cached login/me data the shell already holds), show an "Editar" affordance that swaps the rendered view for a textarea pre-filled with the raw markdown plus a "Salvar" button that PUTs {titulo, conteudo_md} to /api/docs/ajuda/<chave> and re-renders. Provide closeAjuda(). The drawer element may be created lazily by the module on first open.

    Port renderMdSafe and _applyInline exactly from RESEARCH (supporting h1/h2, bold, italic, code, and bullet lists), using document.createElement / textContent / createTextNode only. Clearing a container via innerHTML='' (empty string) is allowed; assigning untrusted content via innerHTML is not.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const s=require('fs').readFileSync('assets/erp-docs.js','utf8'); if(!/'use strict'/.test(s)) throw 'no use strict'; if(!/renderMdSafe/.test(s)) throw 'no safe renderer'; if(!/createTextNode|textContent/.test(s)) throw 'no safe DOM api'; const bad=s.split('\n').filter(l=>/\.innerHTML\s*=/.test(l) && !/innerHTML\s*=\s*''/.test(l) && !/innerHTML\s*=\s*\x22\x22/.test(l)); if(bad.length) throw 'unsafe innerHTML assignment: '+bad.join(' | '); if(!/api\/docs\/documentos/.test(s) || !/api\/docs\/ajuda/.test(s)) throw 'missing docs endpoints'; if(!/Bearer/.test(s)) throw 'no bearer token'; console.log('OK erp-docs.js safe + wired')"</automated>
  </verify>
  <done>erp-docs.js exists as an IIFE; renderMdSafe present and the only innerHTML assignments are empty-string clears; the documentos and ajuda endpoints are called with a Bearer token; per-tipo badges and the upload/history/download actions are implemented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Documentos page, help button, and script into cmasm_erp.html</name>
  <files>cmasm_erp.html</files>
  <read_first>
    - cmasm_erp.html lines 633-660 — the Infraestrutura sidebar nav items (insert the Documentos nav near the Ativos/Estoque entries)
    - cmasm_erp.html line ~774 — the .sb-tools div holding .topbar-alert buttons (insert the help "?" button here)
    - cmasm_erp.html line ~1416 — the page-manutencao div pattern (add a sibling page-documentos div with class="page")
    - cmasm_erp.html lines 544-548 — the deferred <script src="assets/..."> block (append the erp-docs.js tag, ordered after pmoc-engine.js)
    - cmasm_erp.html line ~3050 — showPage(page, el) to understand how a page is activated and the active nav toggled
  </read_first>
  <action>
    Make four additive edits to cmasm_erp.html (do NOT remove or reorder existing nav items, pages, or scripts):
    (1) Add a sidebar nav item in the Infraestrutura section (near Ativos/Estoque): a div.ni with data-page="documentos" and onclick="showPage('documentos',this)" with a document-style SVG icon and the label "Documentos".
    (2) Add an empty page container: a div with id="page-documentos" class="page" (sibling of page-manutencao); have it (or a small init in the inline script) call window.erpDocs render/mount when shown — follow exactly how page-manutencao is populated by erp-manutencao.js (e.g. mount on first showPage('documentos') or on DOMContentLoaded into #page-documentos).
    (3) Add a help "?" button inside the .sb-tools div (line ~774) as a button.topbar-alert (matching the sibling alert buttons) whose onclick calls window.erpDocs.openAjuda(<current page key>) — derive the current page key from the active nav/page (the same value showPage uses), defaulting to 'geral'.
    (4) Append a deferred script tag <script src="assets/erp-docs.js" defer></script> to the script block (after pmoc-engine.js so window.engine.utils.el is available).
    Ensure the inline glue passes the active page's data-page value as the ajuda chave so the drawer is contextual.
  </action>
  <verify>
    <automated>cd /home/luc/DEV_ERP/cmasm.erp && node -e "const s=require('fs').readFileSync('cmasm_erp.html','utf8'); if(!/data-page=\x22documentos\x22/.test(s)) throw 'no documentos nav'; if(!/id=\x22page-documentos\x22/.test(s)) throw 'no documentos page'; if(!/erp-docs\.js/.test(s)) throw 'script not included'; if(!/erpDocs\.openAjuda/.test(s)) throw 'help button not wired'; console.log('OK cmasm_erp.html wired')"</automated>
  </verify>
  <done>The Documentos nav item, page-documentos container, help "?" button, and erp-docs.js script tag are all present; no existing nav/page/script was removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Documentos page + contextual help drawer</name>
  <action>Pause execution and present the how-to-verify steps below to the human; do not proceed until they reply "approved" or report issues to fix.</action>
  <what-built>Documentos repository page (browse by categoria+tipo, +Novo documento, upload de versão, histórico, download, badges per tipo) and a contextual help "?" drawer with safe markdown rendering and a manager edit mode — backed by the /api/docs endpoints.</what-built>
  <how-to-verify>
    1. Serve the app (e.g. `python3 -m http.server 8080`) and start the backend (`uvicorn backend.main:app --port 8010 --reload`); open http://localhost:8080/cmasm_erp.html and log in as a write-capable user.
    2. Click "Documentos" in the sidebar — the page loads with a categoria selector and tipo filter.
    3. Create a documento (+ Novo documento) under categoria "geral", tipo "modelo"; it appears in the list with a tipo badge.
    4. Upload a small PDF as a version — it shows versão 1; upload again — versão 2; open Histórico and confirm both versions with autor + data, oldest preserved.
    5. Click Baixar on version 1 — the downloaded file is the v1 file (not v2).
    6. Try uploading a .exe — the UI surfaces a rejection (4xx), no version is added.
    7. Click the "?" help button while on a page — the drawer opens showing that page's help text rendered (bold/lists visible, no raw markup); as a manager, click Editar, change the text, Salvar, and confirm it persists on reopen.
    8. (Optional) Log in as a visualizador and confirm write actions (create/upload/save help) are blocked (403 surfaced).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server ajuda content → DOM | conteudo_md from the DB is rendered into the help drawer |
| browser file picker → upload | user-selected file bytes leave for the multipart endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08F-01 | Tampering/Elevation | XSS via ajuda markdown rendered with innerHTML | high | mitigate | renderMdSafe builds DOM with createElement/textContent/createTextNode only; the verify gate greps for any non-empty-string innerHTML assignment and fails the task |
| T-08F-02 | Information Disclosure | unauthenticated docs/help fetches | medium | mitigate | authFetch attaches the Bearer token to every /api/docs request; backend (Plan 01) enforces _require_auth |
| T-08F-03 | Elevation of Privilege | visualizador sees edit/upload controls | low | mitigate | edit/upload affordances gated on role != visitante client-side; backend _require_escrita is the authoritative 403 (defense in depth) |
</threat_model>

<verification>
- erp-docs.js: 'use strict', renderMdSafe present, no non-empty innerHTML assignment, Bearer on fetches, docs+ajuda endpoints called.
- cmasm_erp.html: documentos nav item, page-documentos container, help button wired to erpDocs.openAjuda, erp-docs.js script tag — all present; no existing nav/page/script removed.
- Human-verify checkpoint passes: browse/upload/version/download/help-edit all work; .exe rejected; visualizador blocked.
</verification>

<success_criteria>
- DOC-01: contextual help drawer renders + edits the current page topic safely.
- DOC-02: documentos page browses, uploads versions, shows history, downloads.
- DOC-03: tipo/categoria filtering browses norms and guias per category.
- No untrusted innerHTML; Bearer on all calls; no regression to existing pages.
</success_criteria>

<output>
Create `.planning/phases/08-ajuda-e-documentacao/08-03-SUMMARY.md` when done.
</output>
