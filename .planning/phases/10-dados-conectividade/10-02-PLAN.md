---
phase: 10-dados-conectividade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/backfill_estrutura_id.py
autonomous: true
requirements: [CON-01]
must_haves:
  truths:
    - "Executar tools/backfill_estrutura_id.py popula locais.estrutura_id apenas nas linhas cujo codigo casa exatamente com um estrutura.id, pulando (sem falhar) as demais (CON-01, D-04)."
    - "Reexecutar o script não sobrescreve estrutura_id já preenchido nem duplica trabalho — só toca linhas com estrutura_id IS NULL (idempotente)."
    - "O script imprime o número de linhas populadas e o número de órfãos (locais sem match), fixando a expectativa de 0 matches hoje como resultado CORRETO (D-04b)."
    - "As consultas de organização em main.py resolvem por estrutura_id quando presente, via o COALESCE(l.estrutura_id, l.codigo) já existente (CON-01)."
  artifacts:
    - "tools/backfill_estrutura_id.py — script idempotente de backfill locais.codigo → estrutura.id"
  key_links:
    - "locais.estrutura_id → estrutura.id — FK que o COALESCE em main.py:1966-1970 e :1988-1990 usa como primeira opção"
  prohibitions:
    - "NÃO criar nós sintéticos em estrutura para casar órfãos — locais sem match ficam com estrutura_id NULL e entram no relatório de integridade (CON-06)."
    - "NÃO prometer 'N/163 populado com N>0' como sucesso — o critério é 'mecanismo idempotente + órfãos visíveis' (D-04b, Pitfall 1)."
    - "NÃO remover o COALESCE(estrutura_id, codigo) — ele é o fallback que sobrevive enquanto houver órfãos."
    - "NÃO deletar nem editar locais.codigo (correção REFRI-→CMASM é deferida a Future Requirements)."
---

<objective>
Entregar o MECANISMO de conectividade de organização do CON-01: um backfill idempotente que mapeia locais.codigo → estrutura.id e confirma que os org joins de main.py já resolvem por FK via o COALESCE existente. Aceita explicitamente 0/163 matches hoje (D-04b) — os dados reais têm locais.codigo em formato REFRI-<AREA>-<NOME> e estrutura.id em CMASM-XX.Y, então 0 casam por igualdade de string; isso é o comportamento correto, não uma falha.

Purpose: Destravar a resolução de organização por relacionamento real. O mecanismo fica pronto e correto; quando/se locais.codigo receber valores CMASM-XX.Y no futuro, o backfill passa a popular sem mudança de código.
Output: Script de backfill idempotente + verificação do cutover do join.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@backend/main.py
</context>

## Artifacts this phase produces (deste plano)

- `tools/backfill_estrutura_id.py` — backfill idempotente `locais.codigo → estrutura.id`

<tasks>

<task type="auto">
  <name>Task 1: Criar tools/backfill_estrutura_id.py (backfill idempotente)</name>
  <files>tools/backfill_estrutura_id.py</files>
  <read_first>
    - tools/backfill_local_id.py — padrão de script de backfill existente (conexão sqlite3 direta a data/core.db, print de contagem, idempotência). Espelhar esse padrão de estrutura de arquivo.
    - RESEARCH §3.2 (SQL idempotente exato) e §3.1 (descoberta crítica: 0/163 casam hoje).
    - data/schema_core.sql:64-80 — colunas de locais (codigo UNIQUE, estrutura_id) e estrutura (id).
  </read_first>
  <action>
    Criar tools/backfill_estrutura_id.py, um script standalone idempotente (conexão sqlite3 direta a DB_PATH, default ./data/core.db, mesmo padrão de tools/backfill_local_id.py). Lógica central: executar `UPDATE locais SET estrutura_id = codigo WHERE estrutura_id IS NULL AND codigo IN (SELECT id FROM estrutura)`. Antes e depois do UPDATE, contar e imprimir: total de locais, quantos com estrutura_id preenchido, quantos órfãos (estrutura_id IS NULL). Imprimir uma nota PT-BR fixando a expectativa: hoje 0 matches é o resultado CORRETO (locais.codigo em REFRI-*, estrutura.id em CMASM-XX.Y — ver D-04b), e os órfãos são reportados pelo relatório de integridade (CON-06). O script NÃO deve falhar quando 0 linhas casam; NÃO criar nós sintéticos; NÃO tocar linhas já linkadas. Aceitar argumento opcional de caminho do DB (sys.argv[1]) com default ./data/core.db. Docstring de módulo em PT-BR citando CON-01/D-04/D-04b.
  </action>
  <acceptance_criteria>
    - `python3 tools/backfill_estrutura_id.py` roda sem exceção e imprime as contagens (total, populados, órfãos).
    - Idempotência: rodar o script duas vezes seguidas — a segunda execução reporta o mesmo número de populados (0 novas alterações) e não lança erro.
    - Behavior de segurança: `python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');print(c.execute('SELECT COUNT(*) FROM estrutura').fetchone()[0])"` permanece inalterado antes/depois (nenhum nó sintético criado).
    - Source assertion: `grep -n "estrutura_id IS NULL" tools/backfill_estrutura_id.py` retorna a cláusula que garante idempotência (só toca não-linkados).
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/backfill_estrutura_id.py && python3 tools/backfill_estrutura_id.py && grep -q "estrutura_id IS NULL" tools/backfill_estrutura_id.py && echo OK</automated>
  </verify>
  <done>Backfill idempotente roda 2x sem erro; só toca linhas não-linkadas; nenhum nó sintético criado.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar cutover do org join por FK (sem mudança de código)</name>
  <files>tools/backfill_estrutura_id.py</files>
  <read_first>
    - backend/main.py:1958-1992 — os dois org joins (list_locais e get_local) já usam `LEFT JOIN estrutura e ON e.id = COALESCE(l.estrutura_id, l.codigo)` e `LEFT JOIN cargos c ON c.unidade_id = COALESCE(l.estrutura_id, l.codigo)`.
    - RESEARCH §3.3 — recomendação: manter o COALESCE como está (resolve por FK quando presente, cai para codigo quando NULL); nenhuma mudança de main.py é necessária nesta fase.
  </read_first>
  <action>
    Confirmar (verificação, não edição de main.py) que os org joins já implementam "resolve por FK quando estrutura_id existe, fallback para codigo enquanto houver órfãos", exatamente o comportamento pedido por D-04. NÃO alterar main.py — o COALESCE existente é o cutover. Adicionar ao rodapé do print do script backfill uma linha reportando quantos locais ainda dependem do fallback codigo (COUNT onde estrutura_id IS NULL), para que uma fase futura saiba quando é seguro remover o COALESCE. Documentar essa contagem como o dado que o CON-06 (plano 10-06) também exporá.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "COALESCE(l.estrutura_id, l.codigo)" backend/main.py` retorna 4 (dois joins × duas queries) — confirmando que o cutover por FK já está no lugar; main.py permanece inalterado (nenhum diff em backend/main.py neste plano).
    - Behavior: `python3 tools/backfill_estrutura_id.py` imprime a contagem de locais dependentes do fallback codigo (hoje esperado: 163).
    - Behavior (join): iniciar `uvicorn backend.main:app --port 8010`, autenticar e `curl -s http://localhost:8010/api/locais` retorna 200 com o campo `estrutura_nome` presente por linha (join resolve sem erro).
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c "COALESCE(l.estrutura_id, l.codigo)" backend/main.py) -eq 4 && echo OK</automated>
  </verify>
  <done>COALESCE join por FK confirmado em main.py (grep=4, sem edição de main.py); contagem de fallback reportada pelo script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script tools/ → SQLite | UPDATE de dados de produção em locais.estrutura_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | UPDATE locais.estrutura_id | medium | mitigate | WHERE estrutura_id IS NULL garante não-sobrescrita; só casa codigo que já existe em estrutura(id); nenhum DELETE/DROP |
| T-10-04 | Repudiation | backfill sem log | low | mitigate | Script imprime contagens antes/depois (populados/órfãos/fallback) para auditoria |
</threat_model>

<verification>
- Backfill idempotente (2ª execução = 0 alterações, sem erro).
- 0 nós sintéticos criados.
- COALESCE join intacto em main.py (grep = 4).
- Órfãos reportados (163 hoje).
</verification>

<success_criteria>
O mecanismo de backfill existe, é idempotente e correto; os org joins resolvem por FK via COALESCE; os órfãos ficam visíveis para o CON-06. Sucesso = mecanismo, não N/163 populado.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-02-SUMMARY.md` when done
</output>
