---
phase: 10-dados-conectividade
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - backend/main.py
  - cmasm_erp.html
autonomous: true
requirements: [CON-02]
must_haves:
  truths:
    - "POST /api/os grava os.lotacao_id na criação: se o corpo não enviar lotacao_id, é derivado da unidade do cargo do solicitante (cargos.usuario_id → cargos.unidade_id) (CON-02, D-03)."
    - "Se o corpo enviar lotacao_id explícito (override do form), esse valor é usado em vez do derivado."
    - "GET /api/os e GET /api/os/{id} retornam lotacao_id automaticamente (SELECT o.* já o inclui) sem mudança nesses handlers."
    - "os.departamento continua sendo gravado como rótulo denormalizado; clientes antigos (PMOC/satélites) que não enviam lotacao_id continuam funcionando."
    - "O form de OS no cmasm_erp.html oferece um seletor opcional de lotação (estrutura) que preenche lotacao_id no POST."
  artifacts:
    - "backend/main.py — campo OSIn.lotacao_id + lógica de auto-fill em create_os + coluna lotacao_id no INSERT"
    - "cmasm_erp.html — seletor opcional de lotação no form de criação de OS"
  key_links:
    - "cargos.usuario_id → cargos.unidade_id → estrutura.id — cadeia do auto-fill server-side"
    - "OSIn.lotacao_id (Optional) → INSERT ordens_servico.lotacao_id → coluna criada no plano 10-01"
  prohibitions:
    - "NÃO tornar lotacao_id obrigatório em OSIn — deve ser Optional para não quebrar o contrato POST /api/os (PMOC/satélites)."
    - "NÃO remover nem parar de gravar os.departamento."
    - "NÃO exigir auth adicional no path de modulo_origem (RES-05: externos postam sem token interativo)."
---

<objective>
Fazer POST /api/os gravar a lotação como FK real (os.lotacao_id → estrutura), preenchida automaticamente da unidade do cargo do solicitante, com override opcional por seletor no form da OS. os.departamento permanece como rótulo denormalizado e o contrato de API existente não quebra (CON-02, D-03).

Purpose: Lotação passa a resolver por relacionamento (estrutura), não por string livre em departamento.
Output: Auto-fill server-side + seletor opcional client-side, ambos gravando lotacao_id.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@backend/main.py
</context>

## Artifacts this phase produces (deste plano)

- `OSIn.lotacao_id: Optional[str]` (backend/main.py)
- Auto-fill de lotação em `create_os` a partir de `cargos` (backend/main.py)
- Coluna `lotacao_id` incluída no INSERT de `ordens_servico` (backend/main.py)
- Seletor opcional de lotação no form de OS (cmasm_erp.html)

<tasks>

<task type="auto">
  <name>Task 1: OSIn.lotacao_id + auto-fill + INSERT em create_os (backend)</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py:924-937 — classe OSIn (campos existentes, incluindo solicitante_id e departamento Optional).
    - backend/main.py:2126-2152 — create_os (guard condicional por modulo_origem; INSERT ordens_servico com a lista de colunas e VALUES).
    - backend/main.py:1130-1137 — padrão cargos.usuario_id → cargos.unidade_id já usado em list_unidades (fonte do auto-fill).
    - RESEARCH §2.2 e §2.3 (auto-fill snippet + garantia de não quebra de contrato).
    - Depende do plano 10-01 (coluna ordens_servico.lotacao_id já criada).
  </read_first>
  <action>
    Em backend/main.py:
    1. Em OSIn (:924-937), adicionar `lotacao_id: Optional[str] = None` (override explícito do form; auto-derivado se ausente). Comentário PT-BR citando CON-02/D-03.
    2. Em create_os (:2126-2152), ANTES do INSERT, computar a lotação: usar `body.lotacao_id` se enviado; caso contrário, se `body.solicitante_id` existir, derivar via `SELECT unidade_id FROM cargos WHERE usuario_id = ?` (db.fetch_one) e usar `unidade_id` se houver linha, senão None. Reaproveitar exatamente o padrão de :1130-1137.
    3. Adicionar `lotacao_id` à lista de colunas do INSERT de ordens_servico e o valor computado à tupla VALUES (mais um placeholder `?`). Manter `departamento` como está (continua gravado).
    Não alterar GET /api/os / get_os — `SELECT o.*` já devolve lotacao_id automaticamente.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "lotacao_id" backend/main.py` mostra (a) o campo em OSIn, (b) a derivação via cargos, (c) a coluna no INSERT.
    - Behavior (auto-fill): iniciar `uvicorn backend.main:app --port 8010`; autenticar; `POST /api/os` com um `solicitante_id` que ocupa cargo (cargos.usuario_id preenchido) e SEM lotacao_id no corpo → a OS criada (GET /api/os/{id}) retorna `lotacao_id` = a `unidade_id` do cargo do solicitante.
    - Behavior (override): `POST /api/os` com `lotacao_id` explícito no corpo → a OS retorna esse valor exato.
    - Behavior (não quebra): `POST /api/os` com `modulo_origem` setado e SEM lotacao_id nem solicitante_id → retorna 200/201 (contrato externo intacto); a OS retorna `lotacao_id` NULL e `departamento` conforme enviado.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "lotacao_id" backend/main.py && grep -q "SELECT unidade_id FROM cargos WHERE usuario_id" backend/main.py && echo OK</automated>
  </verify>
  <done>OSIn.lotacao_id Optional; auto-fill via cargos; coluna no INSERT; departamento e contrato externo preservados.</done>
</task>

<task type="auto">
  <name>Task 2: Seletor opcional de lotação no form de OS (frontend)</name>
  <files>cmasm_erp.html</files>
  <read_first>
    - cmasm_erp.html — localizar o form de criação de OS (buscar por `criarOS`/`novaOS`/o payload que chama `sdk.os.create`/POST /api/os) e o padrão de dropdowns de estrutura/unidades já usados na tela de organização.
    - backend/main.py — endpoint que lista estrutura/unidades (GET /api/estrutura ou list_unidades) para popular o seletor.
  </read_first>
  <action>
    No form de criação de OS do cmasm_erp.html, adicionar um campo `<select>` OPCIONAL "Lotação" populado com as unidades de estrutura (reaproveitar a fonte de dados de unidades já usada na tela de organização; não criar novo endpoint). Ao submeter a OS, incluir `lotacao_id` no corpo do POST somente se o usuário selecionou um valor (campo vazio = não enviar, deixando o auto-fill server-side agir). Rótulo e estilo seguindo o tema dark existente (tokens --bg/--acc; sem novo framework). Comentário PT-BR: "// CON-02 (D-03) — override opcional da lotação; vazio => auto-fill pelo cargo do solicitante".
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "lotacao_id" cmasm_erp.html` mostra o campo do form e a inclusão condicional no payload do POST.
    - Behavior: abrir o ERP servido, abrir o form de nova OS — existe um seletor "Lotação" opcional; criar OS deixando-o vazio grava lotacao_id via auto-fill; criar OS selecionando uma unidade grava esse valor (verificável em GET /api/os/{id}).
    - Behavior (smoke): balanço de chaves do arquivo permanece válido (o form ainda renderiza sem erro no console do browser).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "lotacao_id" cmasm_erp.html && echo OK</automated>
  </verify>
  <done>Seletor opcional de lotação no form de OS; vazio => auto-fill; valor selecionado grava lotacao_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente/externo → POST /api/os | Corpo pode conter lotacao_id fornecido pelo cliente |
| browser → SQLite (via API) | Auto-fill server-side lê cargos do solicitante |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Spoofing | lotacao_id no corpo | low | accept | SQLite não impõe FK por padrão no repo (consistente com o schema atual); lotacao_id inválido fica solto sem quebrar — o CON-06 sinaliza inconsistências |
| T-10-06 | Tampering | INSERT ordens_servico | low | mitigate | Query parametrizada (aiosqlite bind), sem interpolação de string; padrão já usado no repo |
| T-10-07 | Elevation of Privilege | create_os | low | mitigate | Guard existente `_require_auth` + `_require_escrita` no path interno preservado; path externo por modulo_origem inalterado (RES-05) |
</threat_model>

<verification>
- lotacao_id auto-preenchido do cargo do solicitante.
- Override explícito respeitado.
- Contrato externo (modulo_origem) intacto.
- os.departamento preservado.
</verification>

<success_criteria>
POST /api/os grava lotacao_id (auto ou override); departamento permanece; contratos existentes não quebram; seletor opcional funciona no form.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-03-SUMMARY.md` when done
</output>
