---
phase: 10-dados-conectividade
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - backend/grama.py
  - tools/backfill_grama_link.py
autonomous: true
requirements: [CON-04]
must_haves:
  truths:
    - "Ao concluir uma operação de grama (PUT /api/grama/operacoes/{id}/status com status=concluido), as horas são somadas em ativos.uso_atual — ativos passa a ser a fonte única de horas (CON-04, D-01, Rules.md §3)."
    - "GET /api/grama/maquinas devolve o shape esperado por normalizeVegMaq lendo ativos (categoria maquinas_corte) com metadados (fabricante/modelo/serie/ano/combustivel) via LEFT JOIN grama_maquinas ON grama_maquinas.id = ativos.grama_maquina_id; horas_uso exposto = ativos.uso_atual."
    - "O backfill de link casa 1:1 apenas os casos inequívocos (MS650, SOL) preenchendo ativos.grama_maquina_id; grupos ambíguos (FS220/GAR/TS114) NÃO recebem pareamento e começam com uso_atual=0 (CON-04, D-02)."
    - "grama_maquinas nunca é DROP — permanece como satélite de metadados (fabricante, série, combustível)."
  artifacts:
    - "backend/grama.py — write de horas repontado para ativos.uso_atual em update_operacao_status; list_maquinas/get_maquina lendo ativos + JOIN grama_maquinas"
    - "tools/backfill_grama_link.py — backfill idempotente que linka ativos.grama_maquina_id para os matches 1:1 (MS650, SOL)"
  key_links:
    - "grama_operacoes_servico.maquina_id → ativos.id (semântica futura) → UPDATE ativos.uso_atual"
    - "ativos.grama_maquina_id → grama_maquinas.id — link satélite para metadados no JOIN de list_maquinas"
  prohibitions:
    - "NUNCA DROP grama_maquinas — só deixa de ser fonte de horas/identidade; metadados e combustível continuam nela."
    - "NÃO fazer pareamento ordinal dos grupos ambíguos FS220/GAR/TS114 (D-02 travado: sem pareamento; uso_atual começa 0; histórico grama_maquinas fica arquival)."
    - "NÃO migrar combustível para ativos — combustivel_atual continua em grama_maquinas (Pitfall 3; fora do escopo de 'uso_atual fonte única')."
    - "NÃO fazer grep negativo file-wide por 'UPDATE grama_maquinas' — o fluxo de combustível (create_combustivel) legitimamente ainda escreve nessa tabela."
---

<objective>
Aposentar grama_maquinas como cadastro-mestre de máquinas de corte, mantendo-a como satélite de metadados: unificar as horas em ativos.uso_atual (fonte única, Rules.md §3), repontar a escrita crítica de update_operacao_status e servir GET /api/grama/maquinas a partir de ativos com metadados via JOIN. Linkar apenas os matches inequívocos (MS650, SOL); grupos ambíguos não são pareados (D-02).

Purpose: Eliminar o cadastro duplo de horas entre grama_maquinas e ativos; ativos vira a verdade.
Output: Repoint do write + read por JOIN + backfill de link 1:1 idempotente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@backend/grama.py
</context>

## Artifacts this phase produces (deste plano)

- Repoint do write de horas para `ativos.uso_atual` em `update_operacao_status` (backend/grama.py)
- `list_maquinas`/`get_maquina` lendo `ativos` + `LEFT JOIN grama_maquinas` (backend/grama.py)
- `tools/backfill_grama_link.py` — backfill idempotente de `ativos.grama_maquina_id` (matches 1:1 MS650, SOL)

<tasks>

<task type="auto">
  <name>Task 1: Repontar horas para ativos.uso_atual e servir maquinas por JOIN (grama.py)</name>
  <files>backend/grama.py</files>
  <read_first>
    - backend/grama.py:726-752 — update_operacao_status: hoje o bloco (linhas ~744-751) faz `UPDATE grama_maquinas SET horas_uso=horas_uso+?, combustivel_atual=... WHERE id=(SELECT maquina_id ...)`. Esta é a escrita crítica a repontar.
    - backend/grama.py:541-558 — list_maquinas/get_maquina (hoje `SELECT * FROM grama_maquinas`).
    - RESEARCH §1.4 (mapeamento por endpoint), §1.5 (campos sem equivalente + JOIN proposto), §1.6 (shape que normalizeVegMaq espera) e Pitfall 2 (perda de granularidade de status) e Pitfall 3 (combustível fica).
    - cmasm_erp.html:7986-8000 — normalizeVegMaq (campos consumidos: id, nome, tipo, fabricante, modelo, numero_serie, ano_fabricacao, status, horas_uso, observacoes).
    - Depende do plano 10-01 (coluna ativos.grama_maquina_id já criada).
  </read_first>
  <action>
    Em backend/grama.py:
    1. update_operacao_status (~:744-751): SEPARAR as duas escritas. As HORAS passam a ir para ativos: `UPDATE ativos SET uso_atual = uso_atual + ? WHERE id = (SELECT maquina_id FROM grama_operacoes_servico WHERE id=?)` (fonte única, Rules.md §3). O decremento de combustível PERMANECE em grama_maquinas via o link legado: `UPDATE grama_maquinas SET combustivel_atual = MAX(0, combustivel_atual - ?) WHERE id = (SELECT g.grama_maquina_id FROM ativos g WHERE g.id = (SELECT maquina_id FROM grama_operacoes_servico WHERE id=?))` — ou, se o maquina_id ainda apontar para grama_maquinas nos dados legados (0 linhas hoje), manter o combustível na tabela satélite sem duplicar identidade. NÃO migrar combustível para ativos.
    2. list_maquinas/get_maquina (:541-558): trocar a origem para `SELECT a.id, a.nome, a.tipo, a.uso_atual AS horas_uso, gm.fabricante, gm.modelo, gm.numero_serie, gm.ano_fabricacao, gm.combustivel_tipo, gm.combustivel_capacidade, gm.combustivel_atual, a.obs AS observacoes, CASE WHEN a.ativo=1 THEN 'operacional' ELSE 'inativo' END AS status FROM ativos a LEFT JOIN grama_maquinas gm ON gm.id = a.grama_maquina_id WHERE a.categoria='maquinas_corte'` (+ filtro opcional por tipo; o filtro por status passa a comparar o valor derivado). Preservar o shape exato de normalizeVegMaq. Comentar em PT-BR a perda aceita de granularidade de status (Pitfall 2: 4 estados → 2) como fora do escopo desta fase.
    Não alterar create_combustivel/list_combustivel (combustível continua na satélite).
  </action>
  <acceptance_criteria>
    - Source assertion (positivo): `grep -n "UPDATE ativos SET uso_atual = uso_atual" backend/grama.py` retorna a linha do repoint em update_operacao_status.
    - Source assertion (read por JOIN): `grep -n "LEFT JOIN grama_maquinas" backend/grama.py` retorna o JOIN em list_maquinas/get_maquina.
    - Behavior (fonte única): iniciar `uvicorn backend.main:app --port 8010`; criar uma operação de grama para um ativo maquinas_corte, concluí-la (PUT status=concluido, horas_utilizadas>0); GET /api/ativos/{id} mostra `uso_atual` incrementado pelas horas; o valor de horas em grama_maquinas NÃO é mais a fonte lida pelo GET /api/grama/maquinas.
    - Behavior (shape preservado): `curl -s http://localhost:8010/api/grama/maquinas` retorna itens com as chaves id, nome, tipo, fabricante, modelo, numero_serie, ano_fabricacao, status, horas_uso, observacoes.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "UPDATE ativos SET uso_atual = uso_atual" backend/grama.py && grep -q "LEFT JOIN grama_maquinas" backend/grama.py && echo OK</automated>
  </verify>
  <done>Horas somam em ativos.uso_atual (fonte única); list_maquinas serve por JOIN preservando o shape de normalizeVegMaq.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill idempotente de ativos.grama_maquina_id (matches 1:1)</name>
  <files>tools/backfill_grama_link.py</files>
  <read_first>
    - RESEARCH §1.2 (matriz de matching) e §1.3 (só MS650 e SOL são 1:1; ambíguos NÃO pareados; BR600 sem ativo).
    - tools/backfill_local_id.py — padrão de script de backfill (sqlite3 direto, prints, idempotência).
    - Depende do plano 10-01 (ativos.grama_maquina_id existe).
  </read_first>
  <action>
    Criar tools/backfill_grama_link.py, script standalone idempotente (default DB ./data/core.db). Para cada modelo de grama_maquinas cujo grupo é INEQUÍVOCO (exatamente 1 grama_maquina e exatamente 1 ativo do tipo correspondente): setar `ativos.grama_maquina_id = <gmaq.id>` no ativo casado, SOMENTE onde `ativos.grama_maquina_id IS NULL` (idempotente). Na prática isso casa apenas: modelo MS650 → ativos.tipo='MS650'; modelo SOL-1200 → ativos.tipo='SOL'. Determinar "inequívoco" por contagem (COUNT grama por modelo/tipo == 1 E COUNT ativos por tipo == 1), NÃO por atribuição ordinal. Para os grupos ambíguos (FS220, GAR-53, TS114) e para o soprador BR600 (sem tipo em ativos): NÃO linkar — imprimir que ficam sem pareamento (uso_atual começa 0; histórico grama_maquinas arquival), conforme D-02. Imprimir resumo: quantos linkados, quantos ambíguos pulados, quantos sem-equivalente. Docstring PT-BR citando CON-04/D-01/D-02.
  </action>
  <acceptance_criteria>
    - `python3 tools/backfill_grama_link.py` roda sem exceção e imprime o resumo (linkados / ambíguos pulados / sem-equivalente).
    - Behavior (matches exatos): após rodar, `python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');print(c.execute(\"SELECT COUNT(*) FROM ativos WHERE grama_maquina_id IS NOT NULL\").fetchone()[0])"` == 2 (MS650 + SOL) dado o dataset atual.
    - Behavior (ambíguos não pareados): nenhum ativo de tipo FS220/GAR/TS114 recebe grama_maquina_id (verificável por query filtrando esses tipos = 0 linkados).
    - Idempotência: segunda execução reporta 0 novos links e não lança erro; nenhum DROP/DELETE em grama_maquinas (`SELECT COUNT(*) FROM grama_maquinas` inalterado, = 12).
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/backfill_grama_link.py && python3 tools/backfill_grama_link.py && python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');assert c.execute('SELECT COUNT(*) FROM ativos WHERE grama_maquina_id IS NOT NULL').fetchone()[0]==2 and c.execute('SELECT COUNT(*) FROM grama_maquinas').fetchone()[0]==12;print('OK')"</automated>
  </verify>
  <done>Backfill linka só MS650+SOL (1:1); ambíguos não pareados; idempotente; grama_maquinas intacta (12 linhas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PUT /api/grama/operacoes/{id}/status → SQLite | Escrita de horas cruza para ativos.uso_atual |
| script tools/ → SQLite | Backfill de link ativos.grama_maquina_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Tampering | UPDATE ativos.uso_atual | medium | mitigate | Query parametrizada; incremento derivado de operação registrada; nenhuma perda de dados em grama_maquinas (nunca DROP) |
| T-10-09 | Repudiation | link heurístico grama↔ativos | low | mitigate | Só links 1:1 determinísticos (contagem==1); ambíguos explicitamente não pareados (D-02) — sem suposição de dados |
| T-10-10 | Information Disclosure | GET /api/grama/maquinas | low | accept | Mesmo shape/consumo já existente; JOIN não expõe campo novo sensível |
</threat_model>

<verification>
- Horas somam em ativos.uso_atual (fonte única).
- list_maquinas serve por JOIN preservando o shape.
- Backfill linka só MS650+SOL; ambíguos pulados; idempotente.
- grama_maquinas nunca DROP (12 linhas intactas).
</verification>

<success_criteria>
ativos.uso_atual é a fonte única de horas; grama_maquinas vira satélite de metadados ligado por ativos.grama_maquina_id; matches inequívocos linkados; contrato de resposta de /api/grama/maquinas preservado.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-04-SUMMARY.md` when done
</output>
