---
phase: 10-dados-conectividade
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - tools/archive_orphan_plano.py
  - Rules.md
autonomous: true
requirements: [CON-05]
must_haves:
  truths:
    - "Executar tools/archive_orphan_plano.py arquiva o plano órfão plano-3c349c22f4 (aplicavel_tipos='[]') via ativo=0 + arquivado_motivo, nunca DROP (CON-05, D-07)."
    - "Após arquivado, GET /api/sync/manifest não inclui mais esse plano (sync.py filtra WHERE ativo=1)."
    - "Reexecutar o script não sobrescreve arquivado_motivo já preenchido (idempotente)."
    - "Rules.md documenta planos_manutencao (0 linhas) como APOSENTADO/legado intencional, mantido no schema por compatibilidade e nunca DROP."
  artifacts:
    - "tools/archive_orphan_plano.py — script idempotente que arquiva o plano climatização órfão"
    - "Rules.md — nota em §11 documentando planos_manutencao como legado aposentado"
  key_links:
    - "catalogo_planos.arquivado_motivo (coluna criada no plano 10-01) + ativo=0 → filtrado por sync.py:519 (WHERE ativo=1)"
  prohibitions:
    - "NUNCA DROP nem DELETE — arquivar via flag (ativo=0 + arquivado_motivo)."
    - "NÃO tocar os 12 planos plano-clima-g1..g12 (aplicavel_tipos=NULL = 'não restringido', semanticamente diferente de '[]')."
---

<objective>
Limpar os dados órfãos do CON-05 de forma não-destrutiva: arquivar o único plano de climatização com aplicavel_tipos='[]' (aplicável a nada) via flag, e documentar planos_manutencao (0 linhas) como legado aposentado em Rules.md. Nenhuma linha deletada — só marcada/documentada (D-07).

Purpose: Remover ruído órfão do manifest sem perder rastro histórico.
Output: Script de arquivamento idempotente + documentação de legado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@Rules.md
</context>

## Artifacts this phase produces (deste plano)

- `tools/archive_orphan_plano.py` — arquiva `plano-3c349c22f4` via `ativo=0` + `arquivado_motivo` (idempotente)
- Nota em `Rules.md` §11 documentando `planos_manutencao` como APOSENTADO

<tasks>

<task type="auto">
  <name>Task 1: Script idempotente de arquivamento do plano órfão (CON-05)</name>
  <files>tools/archive_orphan_plano.py</files>
  <read_first>
    - RESEARCH §6.1 (linha exata: id='plano-3c349c22f4', aplicavel_tipos='[]', ativo=1) e §6.2 Opção B (recomendada: ativo=0 + arquivado_motivo).
    - tools/backfill_local_id.py — padrão de script (sqlite3 direto, prints, idempotência).
    - Depende do plano 10-01 (coluna catalogo_planos.arquivado_motivo criada).
  </read_first>
  <action>
    Criar tools/archive_orphan_plano.py, script standalone idempotente (default DB ./data/core.db). Executar `UPDATE catalogo_planos SET ativo=0, arquivado_motivo='CON-05: aplicavel_tipos=[] — sem ativos correspondentes' WHERE id='plano-3c349c22f4' AND arquivado_motivo IS NULL`. A cláusula `arquivado_motivo IS NULL` garante idempotência (não sobrescreve se já arquivado). Imprimir se arquivou agora ou se já estava arquivado. NÃO deletar; NÃO tocar plano-clima-g1..g12 (aplicavel_tipos=NULL é diferente de '[]'). Docstring PT-BR citando CON-05/D-07 e a distinção NULL vs '[]'.
  </action>
  <acceptance_criteria>
    - `python3 tools/archive_orphan_plano.py` roda sem exceção.
    - Behavior: `python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');print(c.execute(\"SELECT ativo, arquivado_motivo FROM catalogo_planos WHERE id='plano-3c349c22f4'\").fetchone())"` mostra `ativo=0` e `arquivado_motivo` não-nulo.
    - Behavior (não afeta vizinhos): `SELECT COUNT(*) FROM catalogo_planos WHERE id LIKE 'plano-clima-g%' AND ativo=1` permanece inalterado (os 12 planos g1..g12 continuam ativos).
    - Behavior (some do manifest): após arquivar e reiniciar o backend, `GET /api/sync/manifest?modulo=pmoc_refrigeracao` (ou o módulo de climatização) não lista o plano plano-3c349c22f4.
    - Idempotência: segunda execução reporta "já arquivado" e não altera arquivado_motivo.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/archive_orphan_plano.py && python3 tools/archive_orphan_plano.py && python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');r=c.execute(\"SELECT ativo,arquivado_motivo FROM catalogo_planos WHERE id='plano-3c349c22f4'\").fetchone();assert r[0]==0 and r[1];print('OK')"</automated>
  </verify>
  <done>Plano órfão arquivado (ativo=0 + motivo), some do manifest; vizinhos g1..g12 intactos; idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar planos_manutencao como legado aposentado (Rules.md)</name>
  <files>Rules.md</files>
  <read_first>
    - Rules.md §11 — bloco conceitual de planos (onde entra a nota).
    - RESEARCH §6.3 (texto sugerido: planos_manutencao 0 linhas, APOSENTADO, modelo vivo = catalogo_planos + catalogo_plano_itens, nunca DROP).
  </read_first>
  <action>
    Em Rules.md §11 (logo após o bloco de schema conceitual de planos), adicionar uma nota explícita em PT-BR: `planos_manutencao` — APOSENTADO: nunca populada em produção (0 linhas); o modelo de planos vivo é `catalogo_planos` + `catalogo_plano_itens`; mantida no schema por compatibilidade retroativa, nunca DROP. Também registrar que o plano órfão `plano-3c349c22f4` (climatização, aplicavel_tipos vazio) foi arquivado via flag `ativo=0` + `arquivado_motivo` (CON-05/D-07). Edição de documentação apenas — sem mudança de código/dados aqui.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "planos_manutencao" Rules.md` retorna a nota de legado; `grep -ni "APOSENTAD" Rules.md` inclui a referência a planos_manutencao.
    - Source assertion: `grep -n "arquivado_motivo" Rules.md` confirma o registro do arquivamento do plano órfão (CON-05).
    - Behavior: a nota cita explicitamente "nunca DROP" e o par vivo catalogo_planos + catalogo_plano_itens.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "planos_manutencao" Rules.md && grep -qi "APOSENTAD" Rules.md && grep -q "arquivado_motivo" Rules.md && echo OK</automated>
  </verify>
  <done>Rules.md documenta planos_manutencao como legado aposentado (nunca DROP) e o arquivamento do plano órfão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script tools/ → SQLite | UPDATE de flag em catalogo_planos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Tampering | UPDATE catalogo_planos | low | mitigate | WHERE id fixo + arquivado_motivo IS NULL; nenhum DELETE/DROP; alvo é uma única linha órfã verificada |
| T-10-12 | Repudiation | motivo de arquivamento | low | mitigate | arquivado_motivo grava a causa (auditável), diferenciando de desativação administrativa comum |
</threat_model>

<verification>
- Plano órfão arquivado (ativo=0 + motivo), some do manifest.
- Vizinhos g1..g12 intactos.
- Idempotente.
- Rules.md documenta planos_manutencao aposentado.
</verification>

<success_criteria>
Plano climatização órfão arquivado via flag (nunca DROP); planos_manutencao documentado como legado em Rules.md; nenhuma linha deletada.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-05-SUMMARY.md` when done
</output>
