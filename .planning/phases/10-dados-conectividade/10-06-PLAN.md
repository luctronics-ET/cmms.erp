---
phase: 10-dados-conectividade
plan: 06
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - backend/main.py
  - cmasm_erp.html
autonomous: true
requirements: [CON-06]
must_haves:
  truths:
    - "GET /api/admin/integridade exige role='admin' (novo helper _require_admin) e retorna 403 para não-admin, 401 sem token (CON-06, D-05)."
    - "O endpoint retorna categorias de inconsistência com contagens e itens: locais sem estrutura_id, estoque sem local_id, ativos com loc sem local_id, OS com ativo_id/servico_id não resolvidos, planos órfãos, e maquinas_corte sem grama_maquina_id."
    - "O painel de integridade renderiza dentro da aba admin existente do cmasm_erp.html (#page-admin / seção admin-only), consumindo o endpoint — auditoria contínua, não script pontual."
  artifacts:
    - "backend/main.py — helper _require_admin(user) + endpoint GET /api/admin/integridade"
    - "cmasm_erp.html — card/painel de integridade na aba admin consumindo /api/admin/integridade"
  key_links:
    - "_require_auth → _require_admin → queries de integridade (§4.2) → JSON agrupado por categoria → painel admin"
    - "as queries dependem das colunas do plano 10-01 (ativos.grama_maquina_id) e do estado pós plano 10-05 (planos órfãos)"
  prohibitions:
    - "NÃO expor /api/admin/integridade sem o gate de role='admin' (Information Disclosure)."
    - "NÃO interpolar strings de usuário nas queries — só literais fixas/parâmetros validados (Tampering)."
    - "NÃO criar uma página nova — pendurar o painel na aba admin existente (#page-admin)."
---

<objective>
Entregar a auditoria contínua de conectividade do CON-06: um endpoint vivo GET /api/admin/integridade (gated por role admin via novo helper _require_admin) que lista as inconsistências de FK/órfãos, e um painel na aba admin existente do cmasm_erp.html que o consome. Fecha o loop da fase: torna visíveis os órfãos que CON-01/CON-04/CON-05 deixam rastreados.

Purpose: Auditoria contínua substitui scripts pontuais; o admin vê o estado de conectividade em tempo real.
Output: Endpoint admin + painel de integridade.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@backend/main.py
</context>

## Artifacts this phase produces (deste plano)

- `_require_admin(user)` — helper de access control (backend/main.py)
- `GET /api/admin/integridade` — endpoint de auditoria (backend/main.py)
- Painel de integridade na aba admin (cmasm_erp.html)

<tasks>

<task type="auto">
  <name>Task 1: _require_admin + GET /api/admin/integridade (backend)</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py:845-863 — _require_auth e _require_escrita (padrão a espelhar para _require_admin).
    - RESEARCH §4.1 (helper _require_admin + padrão de uso) e §4.2 (queries de integridade concretas + shape de resposta JSON agrupado por categoria).
    - Depende do plano 10-01 (ativos.grama_maquina_id existe → a query 6 funciona). Roda na Wave 3 (após 10-03 que também edita main.py).
  </read_first>
  <action>
    Em backend/main.py:
    1. Adicionar o helper `def _require_admin(user: dict) -> None:` que lança `HTTPException(403, "Acesso restrito a administradores")` se `user.get("role") != "admin"` — espelhando _require_escrita.
    2. Adicionar `@app.get("/api/admin/integridade")` que faz `user = await _require_auth(authorization); _require_admin(user)` e executa as queries de RESEARCH §4.2 (todas com literais fixas/parâmetros — sem interpolação de string de usuário): locais sem estrutura_id; estoque sem local_id; ativos com loc preenchido e local_id NULL; ordens_servico com ativo_id não em ativos; ordens_servico com servico_id não em catalogo_servicos; catalogo_planos órfãos (aplicavel_tipos NULL ou '[]' com ativo=1); ativos categoria='maquinas_corte' com grama_maquina_id NULL. Montar a resposta como `{gerado_em, categorias:[{chave, titulo, total, itens}]}` (formato de §4.2), listando itens (não só contagem) para locais órfãos e OS não resolvidas. Comentário PT-BR citando CON-06/D-05.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "_require_admin" backend/main.py` mostra a definição do helper e seu uso no endpoint; `grep -n "/api/admin/integridade" backend/main.py` mostra a rota.
    - Behavior (auth): iniciar `uvicorn backend.main:app --port 8010`; `curl -s -o /dev/null -w "%{http_code}" http://localhost:8010/api/admin/integridade` sem token retorna 401.
    - Behavior (role): com token de usuário NÃO-admin retorna 403; com token de admin retorna 200 com JSON contendo a chave `categorias`.
    - Behavior (conteúdo): a resposta admin inclui as categorias `locais_sem_estrutura` (total esperado hoje: 163) e `estoque_sem_local`, e uma categoria para maquinas_corte sem grama_maquina_id.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "_require_admin" backend/main.py && grep -q "/api/admin/integridade" backend/main.py && echo OK</automated>
  </verify>
  <done>GET /api/admin/integridade gated por role admin (401 sem token, 403 não-admin, 200 admin) com categorias de inconsistência.</done>
</task>

<task type="auto">
  <name>Task 2: Painel de integridade na aba admin (frontend)</name>
  <files>cmasm_erp.html</files>
  <read_first>
    - cmasm_erp.html:724-757 — seção admin-only da sidebar (padrão de navegação admin) e o div de página admin (#page-admin, ~:1514 conforme RESEARCH §4.1).
    - RESEARCH §4.1 (recomendação: novo card dentro de #page-admin; SESSION.role==='admin' já usado no frontend) e §4.2 (shape JSON a renderizar).
    - Padrão de fetch autenticado já usado no cmasm_erp.html (SDK / Authorization Bearer).
  </read_first>
  <action>
    Em cmasm_erp.html, dentro da aba admin existente (#page-admin, sem criar página nova), adicionar um card "Integridade de dados" que faz fetch autenticado a GET /api/admin/integridade e renderiza cada categoria como uma linha com título + badge de contagem, expansível para listar os itens quando houver. Só visível para admin (reaproveitar o gate admin-only / SESSION.role==='admin' já presente). Estilo no tema dark existente (tokens --bg/--acc/--amber/--red; badge vermelho/âmbar para contagens > 0, verde para 0). Comentário PT-BR: "// CON-06 (D-05) — auditoria contínua de conectividade".
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "admin/integridade" cmasm_erp.html` mostra a chamada fetch; `grep -ni "Integridade" cmasm_erp.html` mostra o título do card.
    - Behavior: servir o ERP, logar como admin, abrir a aba admin — o card "Integridade de dados" aparece e lista as categorias com badges de contagem (ex.: locais sem estrutura_id = 163).
    - Behavior (gate): logar como usuário não-admin — o card de integridade não é acessível (consistente com o gate admin-only existente).
    - Behavior (smoke): balanço de chaves do cmasm_erp.html permanece válido; sem erro no console ao abrir a aba admin.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "admin/integridade" cmasm_erp.html && grep -qi "Integridade" cmasm_erp.html && echo OK</automated>
  </verify>
  <done>Painel "Integridade de dados" na aba admin existente consome o endpoint; visível só para admin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → GET /api/admin/integridade | Dados de auditoria interna só para admin |
| browser (aba admin) → API | Painel consome o endpoint com token do admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Elevation of Privilege | GET /api/admin/integridade | high | mitigate | _require_admin: 403 explícito se role != 'admin', após _require_auth; mesmo padrão de _require_escrita |
| T-10-14 | Information Disclosure | relatório de integridade | high | mitigate | Endpoint sempre gated por role admin; nunca exposto sem auth (diferente de rotas públicas da Fase 15) |
| T-10-15 | Tampering | queries de integridade | medium | mitigate | Todas as queries usam literais fixas / parâmetros validados por FastAPI; nenhuma interpolação de input de usuário |
</threat_model>

<verification>
- 401 sem token, 403 não-admin, 200 admin.
- Categorias de integridade presentes com contagens reais (locais 163, estoque 65).
- Painel na aba admin existente consome o endpoint.
- Sem interpolação de string nas queries.
</verification>

<success_criteria>
GET /api/admin/integridade audita conectividade continuamente, gated por role admin; o painel admin exibe as inconsistências; nenhuma nova página criada.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-06-SUMMARY.md` when done
</output>
