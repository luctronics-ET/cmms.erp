---
phase: 10-dados-conectividade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db_core.py
  - data/schema_catalogo.sql
autonomous: true
requirements: [CON-02, CON-03, CON-04, CON-05]
must_haves:
  truths:
    - "Após o startup do backend, PRAGMA table_info(ordens_servico) contém a coluna lotacao_id (CON-02, D-03)."
    - "Após o startup, PRAGMA table_info(ativos) contém a coluna grama_maquina_id (CON-04, D-01)."
    - "Após o startup, PRAGMA table_info(catalogo_planos) contém a coluna arquivado_motivo (CON-05, D-07)."
    - "GET /api/sync/manifest?modulo=fonoclama retorna 10 ativos + 5 planos de fonoclama (CON-03, D-06)."
    - "Reexecutar o startup (re-init) não recria colunas nem falha — migrações idempotentes via PRAGMA table_info."
  artifacts:
    - "backend/db_core.py — 3 novas colunas aditivas (os.lotacao_id, ativos.grama_maquina_id, catalogo_planos.arquivado_motivo)"
    - "data/schema_catalogo.sql — linha de seed INSERT OR IGNORE para o módulo fonoclama"
  key_links:
    - "os.lotacao_id REFERENCES estrutura(id) — base do auto-fill do CON-02 (plano 10-03)"
    - "ativos.grama_maquina_id REFERENCES grama_maquinas(id) — base do link satélite do CON-04 (plano 10-04)"
    - "modulos_registrados.categorias_atend='[\"fonoclama\"]' — chave que faz sync.py:475-486 incluir os 10 ativos fonoclama no manifest"
  prohibitions:
    - "NUNCA usar DROP — só ALTER TABLE ADD COLUMN condicionado por PRAGMA table_info."
    - "NÃO remover nem alterar a coluna os.departamento (permanece como rótulo denormalizado)."
    - "NÃO alterar sync.py — o manifest já é genérico sobre categorias_atend; só falta a linha de seed."
---

<objective>
Estabelecer a fundação de schema da Phase 10: adicionar as 3 colunas aditivas que os planos de lógica downstream (10-03 CON-02, 10-04 CON-04, 10-05 CON-05) consomem, e registrar o módulo fonoclama em modulos_registrados (CON-03) para que o manifest de sync passe a incluir seus 10 ativos + 5 planos.

Purpose: Toda a Phase 10 depende de FKs reais. Este plano cria o esqueleto de colunas (uma única fonte de propriedade sobre backend/db_core.py) para que os planos de lógica rodem em paralelo na Wave 2 sem disputar o arquivo de migrações.
Output: 3 colunas aditivas idempotentes + 1 linha de seed de módulo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dados-conectividade/10-CONTEXT.md
@.planning/phases/10-dados-conectividade/10-RESEARCH.md
@backend/db_core.py
@data/schema_catalogo.sql
</context>

## Artifacts this phase produces (deste plano)

- Coluna `ordens_servico.lotacao_id TEXT REFERENCES estrutura(id)` (CON-02)
- Coluna `ativos.grama_maquina_id TEXT REFERENCES grama_maquinas(id)` (CON-04)
- Coluna `catalogo_planos.arquivado_motivo TEXT` (CON-05)
- Seed `modulos_registrados` linha `('fonoclama', ..., '["fonoclama"]')` (CON-03)

<tasks>

<task type="auto">
  <name>Task 1: Adicionar 3 colunas aditivas em db_core.py (CON-02, CON-04, CON-05)</name>
  <files>backend/db_core.py</files>
  <read_first>
    - backend/db_core.py:57-90 — bloco de migração aditiva existente (os_existing PRAGMA table_info; padrão locais_existing/ddoc_existing) — este é o padrão a espelhar IDENTICAMENTE.
    - data/schema_core.sql:64-80 (locais.estrutura_id já existe) e o CREATE TABLE ativos (uso_atual, categoria) + ordens_servico.
    - RESEARCH §2.1 (snippet os.lotacao_id) e §6.2 Opção B (arquivado_motivo).
  </read_first>
  <action>
    No bloco de migração aditiva de backend/db_core.py (dentro de init(), junto às listas os_existing/locais_existing já presentes), acrescentar três colunas seguindo o padrão condicional PRAGMA table_info + ALTER TABLE ADD COLUMN:
    1. Na lista `os_existing` (ordens_servico), adicionar a tupla ("lotacao_id", "ALTER TABLE ordens_servico ADD COLUMN lotacao_id TEXT REFERENCES estrutura(id)") — logo após a linha existente de "departamento" (CON-02, D-03).
    2. Na lista `existing` de ativos (a que já adiciona local_id), adicionar ("grama_maquina_id", "ALTER TABLE ativos ADD COLUMN grama_maquina_id TEXT REFERENCES grama_maquinas(id)") (CON-04, D-01).
    3. Adicionar um novo bloco `catalogo_planos_existing = {row[1] async for row in await db.execute("PRAGMA table_info(catalogo_planos)")}` com a tupla ("arquivado_motivo", "ALTER TABLE catalogo_planos ADD COLUMN arquivado_motivo TEXT") (CON-05, D-07).
    Cada coluna só é criada se ausente em `existing`/`os_existing`/`catalogo_planos_existing`. Manter tudo antes do `await db.commit()` final. NUNCA DROP. Comentários em PT-BR citando a decisão (ex.: "# CON-02 (D-03) — lotação da OS como FK para estrutura").
  </action>
  <acceptance_criteria>
    - Rodar migrações uma vez: `python3 -c "import asyncio; from backend.db_core import CoreDB; asyncio.run(CoreDB('data/core.db').init())"` completa sem exceção.
    - `python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');cols=lambda t:[r[1] for r in c.execute(f'PRAGMA table_info({t})')];print('lotacao_id' in cols('ordens_servico'), 'grama_maquina_id' in cols('ativos'), 'arquivado_motivo' in cols('catalogo_planos'))"` imprime `True True True`.
    - Reexecutar o comando de init uma segunda vez completa sem exceção e sem erro "duplicate column" (idempotência via PRAGMA table_info).
    - Behavior: `os.departamento` permanece presente (PRAGMA table_info(ordens_servico) ainda contém `departamento`).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import asyncio; from backend.db_core import CoreDB; asyncio.run(CoreDB('data/core.db').init())" && python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');cols=lambda t:[r[1] for r in c.execute(f'PRAGMA table_info({t})')];assert 'lotacao_id' in cols('ordens_servico') and 'grama_maquina_id' in cols('ativos') and 'arquivado_motivo' in cols('catalogo_planos') and 'departamento' in cols('ordens_servico');print('OK')"</automated>
  </verify>
  <done>As 3 colunas aditivas existem; init idempotente (2x sem erro); os.departamento intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar módulo fonoclama no seed (CON-03)</name>
  <files>data/schema_catalogo.sql</files>
  <read_first>
    - data/schema_catalogo.sql:171-224 — CREATE TABLE modulos_registrados + o bloco INSERT OR IGNORE das 7 linhas pmoc_* existentes.
    - backend/sync.py:463-486 — manifest() resolve o módulo por `nome` exato (_modulo_existe) e monta ativos por `categorias_atend`; nenhuma mudança de código é necessária aqui.
    - RESEARCH §5.2 e Assumptions Log A3 (escolha do valor de `nome`).
  </read_first>
  <action>
    Em data/schema_catalogo.sql, no bloco `INSERT OR IGNORE INTO modulos_registrados (nome, descricao, categorias_atend) VALUES` (~:215), acrescentar uma nova linha ao final da lista de valores: nome `'fonoclama'`, descricao `'Sistema de aviso sonoro (fonoclama)'`, categorias_atend `'["fonoclama"]'`. Usar `nome='fonoclama'` (sem prefixo pmoc_) porque o critério de aceitação do ROADMAP chama literalmente `GET /api/sync/manifest?modulo=fonoclama` e `sync.py` resolve o módulo por `nome` exato — o valor de `nome` PRECISA ser `fonoclama`. Manter o padrão INSERT OR IGNORE (idempotente; não duplica se re-seedado). Comentário PT-BR: "# CON-03 (D-06) — registra fonoclama para o manifest incluir os 10 ativos + 5 planos".
  </action>
  <acceptance_criteria>
    - Após re-init (`python3 -c "import asyncio; from backend.db_core import CoreDB; asyncio.run(CoreDB('data/core.db').init())"`), `python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');print(c.execute(\"SELECT categorias_atend FROM modulos_registrados WHERE nome='fonoclama'\").fetchone())"` imprime `('["fonoclama"]',)`.
    - Behavior (manifest): iniciar `uvicorn backend.main:app --port 8010` e `curl -s "http://localhost:8010/api/sync/manifest?modulo=fonoclama"` retorna HTTP 200 com JSON contendo 10 itens em `ativos` (categoria fonoclama) e 5 entradas em `planos`; NÃO retorna 404 "Módulo desconhecido".
    - Idempotência: re-seed (re-init) não cria segunda linha fonoclama — `SELECT COUNT(*) FROM modulos_registrados WHERE nome='fonoclama'` = 1.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import sqlite3;c=sqlite3.connect('data/core.db');assert c.execute(\"SELECT categorias_atend FROM modulos_registrados WHERE nome='fonoclama'\").fetchone()==('[\"fonoclama\"]',);print('OK')"</automated>
  </verify>
  <done>fonoclama registrado; GET /api/sync/manifest?modulo=fonoclama retorna 10 ativos + 5 planos; idempotente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup → SQLite DDL | Migrações executadas no boot alteram o schema de produção |
| cliente PMOC → GET /api/sync/manifest | Cliente de campo (sem token interativo) lê o manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | ALTER TABLE no startup | medium | mitigate | Só ADD COLUMN condicionado por PRAGMA table_info; nunca DROP; idempotente e reversível por ser aditivo |
| T-10-02 | Information Disclosure | manifest fonoclama | low | accept | manifest já expõe categorias registradas ao PMOC por contrato existente; nenhum campo novo sensível é adicionado |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado nesta fase (RESEARCH: Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- Migrações idempotentes: rodar init duas vezes sem erro.
- 3 colunas presentes via PRAGMA table_info.
- fonoclama no manifest (10 ativos + 5 planos).
- os.departamento intacto.
</verification>

<success_criteria>
As 3 colunas aditivas existem e são idempotentes; o módulo fonoclama está registrado e o manifest o inclui. Nenhuma coluna/tabela removida; contrato de sync inalterado.
</success_criteria>

<output>
Create `.planning/phases/10-dados-conectividade/10-01-SUMMARY.md` when done
</output>
