---
phase: 09-limpeza-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .docs_cmasm/referencias/ (8 legacy HTMLs removed)
  - assets/refrig-engine.js
  - assets/erp-manutencao.js
  - backend/manutencao.py
autonomous: true
requirements: [CLN-01]
must_haves:
  truths:
    - "Git tag milestone-import-verificado created at the verified HEAD before any deletion"
    - "The 8 legacy reference HTMLs removed from .docs_cmasm/referencias/ (maps/CSV/screenshots/backups kept)"
    - "grep for Gestao_v2|cmasm13-govbr in assets/ pmoc/ cmasm_erp.html backend/ returns zero"
    - "pytest stays green (comment-only edits; no logic change); imports OK"
  artifacts:
    - "git tag milestone-import-verificado"
---

# Phase 9 Plan 01 — Limpeza Final (executed inline)

Trivial mechanical cleanup (user-confirmed). See 09-01-SUMMARY.md for the executed result.

## Tasks
1. Create git checkpoint tag `milestone-import-verificado` at verified HEAD.
2. Remove the 8 legacy reference HTMLs (CMASM_Gestao_v2, cmasm13-govbr, ata2_carioca, ativo-template, 4 pmoc-*) — permanent (untracked), user-confirmed. Keep maps/CSV/screenshots/db-backups.
3. Strip the deleted-file citations from provenance comments in active code (refrig-engine.js, erp-manutencao.js, manutencao.py) so the criterion grep returns zero, keeping the explanatory intent ("app de campo legado").
4. Verify: grep zero, node --check, python import, no new test regression.
